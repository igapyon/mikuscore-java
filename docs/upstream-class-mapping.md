# Upstream Class Mapping

This document maps upstream `miku-score` files to Java class groups.

The goal is traceability, not Java-first redesign.

## Mapping Status

### Current completion record

The atomic source and case maps cited by the rows below are the authoritative
parity record. Every included class row is now `done`; the long per-row prose
is retained as implementation history, not an open-work list. Local-only
predicates have versioned behavior-equivalent evidence in the MIDI, MEI, and
MuseScore non-unit maps. Browser-only sample constants, VSQX, and Verovio/UI
code remain excluded.

Any later legacy use of “pending”, “partial”, or “remaining” describes an
implementation detail or a superseded intermediate slice; it does not change
the authoritative `done` status above.

### ABC completion record

The legacy `src/ts/abc-io.ts` inventory row below is superseded by
[upstream-abc-io-source-map.md](upstream-abc-io-source-map.md) and the
complete [upstream-abc-io-case-map.md](upstream-abc-io-case-map.md). All
included public exports, 408 pinned I/O cases, the inline-voice case, and the
four pinned golden fixtures are done.

### MIDI completion record

The legacy `src/ts/midi-io.ts` inventory row below is superseded by
[upstream-midi-io-source-map.md](upstream-midi-io-source-map.md) and
[upstream-midi-io-case-map.md](upstream-midi-io-case-map.md). Every
runtime-facing MIDI export, all 70 pinned unit cases, all five pinned runtime
golden cases, and the local-only Moonlight spot predicates are done; see
[upstream-midi-nonunit-case-map.md](upstream-midi-nonunit-case-map.md).

Incremental MUSE evidence: public MSCX import preserves staff-specific numbered
clefs and applies Part `Staff/defaultClef` / `Instrument/clef` staff-ID
precedence before source-measure clefs. Measure-direct TimeSig/KeySig values
also take priority over voice values, with retained cut-time state. Grace and
acciaccatura chords preserve MusicXML grace markers without consuming time.
Note `Tie`/`endSpanner` markers emit MusicXML tie/tied start/stop items, and
known chord articulation/technical subtypes and note fingering/string values
emit leading-note notations. Chord Slur transitions retain staff/voice-local
IDs across measures. Chord-local trill ornaments emit trill and accidental-mark
notations. Event `track`/`move` values route notes and directions to their
MusicXML voice/staff. Key context, explicit accidentals, and `tpc` values
select MusicXML pitch spelling and accidental state. `durationType=measure`
rests span the current measure capacity. ID-referenced tuplets retain written
type, scaled duration, time-modification, and contiguous start/stop notation.
Standalone and Chord-local Ottava spanners retain start/stop numbers across
measures, emit octave-shift directions, and apply their active display pitch
shift during MSCX import.
Standalone and Chord-local Trill spanners retain their shared number across
measures and emit MusicXML trill-mark/wavy-line start and stop notation.
Absolute MuseScore `Tick` events become measure-relative MusicXML voice
`forward` gaps; reverse and cross-lane cursor reconstruction is covered by the
case-mapped Java regressions.
Voice-level repeat `BarLine` events emit a source-positioned middle MusicXML
repeat barline, including `end-start-repeat`.
ID-less inline MuseScore tuplets retain their duration scale, start display
attributes, and `endTuplet` stop notation for chord/rest and nested events;
mixed ID-reference cases are covered by the case-mapped Java regressions.
Lowercase MuseScore `tuplet` ID definitions and chord references follow the
same imported duration/time-modification path as `Tuplet`.
Public MSCX import assigns MusicXML voice numbers part-wide by sorted
staff/local-lane pairs, including `track`/`move` destinations.
MusicXML unpitched display-step/octave notes round trip through MSCX as timed
chord events without becoming rests.
MusicXML octave-shift directions reach public MSCX export as pending Ottava
spanners and return as MusicXML start/stop directions; middle-repeat marks use
the same pending export path and round trip as a source-positioned
`end-start-repeat` voice `BarLine`.
MusicXML trill wavy-lines round trip through public MSCX Trill spanners with
their start/stop number preserved.
Chord-local `Spanner[type=Slur]`, including lowercase `spanner`, shares the
same cross-measure MusicXML slur-number state as legacy `Slur` markers.
Dropped public MSCX chord/rest events retain detailed `mks:diag` context for
unknown duration or missing pitch when `debugMetadata` is enabled.
Unsupported public MSCX voice elements retain one sorted `unsupported-elements`
`mks:diag` warning, while upstream-ignored layout and signature tags stay silent.
MusicXML `trill-mark` without a wavy-line round trips through chord-local MSCX
`ornamentTrill`, not a Trill spanner.
Public MusicXML export has direct regressions for staccato, accent, and tenuto
MSCX articulation subtypes.
An already-dirty `ScoreCore` has direct result-shape evidence for a second
successful dispatch: `dirtyChanged=false` with changed ID, affected measure,
and empty warning/diagnostic lists retained.
Overfull public MSCX voice lanes omit the overflowing tail event and retain a
source-style `mks:diag` `clamped/overfull` payload with occupied and capacity
divisions.
Public MusicXML export has direct regressions for technical stopped/up-bow/
open-string/harmonic plus fingering/string values, and for multi-staff Part
clef scaffolding with an instrument shortName.
Public MusicXML tie/slur export has a direct regression for MSCX Tie/endSpanner
note markers and start/stop Slur span fractions.
Public MSCX import has a direct regression for Dynamic placement in its source
multi-voice MusicXML lane.
Unsupported public MSCX Tuplet definitions retain a source-style
`skipped/unsupported` `mks:diag` entry with measure/staff/voice/tick context.
Unsupported visible Dynamic, Expression, Marker, and Jump events retain the
same source-style `skipped/unsupported` diagnostic context.

| Upstream file | Java class / package | Status | Notes |
| --- | --- | --- | --- |
| `core/ScoreCore.ts` | `jp.igapyon.mikuscore.core.ScoreCore`, `jp.igapyon.mikuscore.musicxml.MusicXmlState` | done | Stateful `load` / `dispatch` / `save` / dirty-state / deterministic note-id / debug-serialization lifecycle is migrated over the existing command backend. The command catalog includes pitch, duration, insert, delete, split, UI noop, timing warnings, save-time integrity validation, finite fractional XML duration validation/occupancy, normalized `editableVoice` options that reject other-voice dispatches and scope overfull save validation, stable node-ID/changed-ID retention across structural edits, and reload node-ID sequencing/failed-reload original-text assignment ordering. Temporary Java IDs use a private name/value marker, avoid document-wide attribute collisions, and pass only their exact selected attribute through the backend; generic MusicXML commands preserve private-looking vendor attributes as markup. Remaining work is exact command-result and validator/XML utility edge behavior. |
| `core/commands.ts` | `jp.igapyon.mikuscore.musicxml.MusicXmlCommandValidation`, `jp.igapyon.mikuscore.musicxml.MusicXmlState` | done | Basic command node-id/no-op behavior migrated |
| `core/interfaces.ts` | `jp.igapyon.mikuscore.core.ScoreCore`, `jp.igapyon.mikuscore.musicxml.MusicXmlCommandValidation` | done | Command validation plus stateful dispatch/save result, warning, diagnostics carriers, immutable Java `ScoreCore.Options` (`editableVoice`), stable node-ID/changed-ID behavior, and JavaScript numeric voice stringification at the unchecked JSON-to-XML boundary are migrated. Its JSON parser now follows JSON-only whitespace as well as number/string grammar; a second successful dispatch on an already-dirty Core retains the Node `dirtyChanged=false` result shape with changed/affected/empty-list fields. Remaining exact command-result details are tracked with `ScoreCore`. |
| `core/validators.ts` | `jp.igapyon.mikuscore.musicxml.MusicXmlState` | done | Basic command payload / target validation, finite JavaScript-number XML duration, timing-context, octave, and alter save validation with occupied-time overfull detection, successful underfull warnings for insert and blocked duration fill, and structural-boundary behavior including Node's strict comparison of a following lane voice against an omitted/null/non-string insert command voice |
| `core/timeIndex.ts` | `jp.igapyon.mikuscore.musicxml.MusicXmlState` | done | Inherited measure capacity, global `backup`/`forward` cursor occupancy, chord-onset exclusion, multi-voice timing, triplet context detection, rest consumption/fill, and split occupied-time invariant are covered. |
| `src/ts/beam-common.ts` | `jp.igapyon.mikuscore.musicxml.MusicXmlIo` | done | `BeamEventInfo`, `BeamAssignment`, beam XML construction, and the full implicit/explicit grouping algorithm are centralized here. The MusicXML, ABC, MIDI, and MuseScore converter paths call it. |
| `src/ts/score-features/durations.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreDurations` | done | Dot-count normalization, MusicXML dot XML item building, and direct MusicXML dot counting are fully covered by the upstream unit intent. |
| `src/ts/score-features/articulations.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreArticulations` | done | Supported kind normalization, stable MusicXML articulation XML item/group building, and direct MusicXML articulation kind extraction are fully covered. Locale-independent lowercasing matches JavaScript behavior. |
| `src/ts/score-features/barlines.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreBarlines` | done | MusicXML barline feature XML building and direct MusicXML barline feature extraction are fully covered, including first direct `bar-style`/typed `ending` selection and locale-independent normalization. |
| `src/ts/score-features/clefs.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreClefs` | done | Clef feature normalization, MusicXML clef XML building, and direct MusicXML clef extraction are fully covered. JavaScript boolean and radix-number coercion are retained for the `line` input. |
| `src/ts/score-features/time-signatures.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreTimeSignatures` | done | Time-signature feature normalization, MusicXML time XML building, and direct MusicXML time signature extraction are fully covered. JavaScript boolean and radix-number coercion are retained for beats and beat type. |
| `src/ts/score-features/key-signatures.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreKeySignatures` | done | Key-signature feature normalization, MusicXML key XML building, and direct MusicXML key signature extraction are fully covered. JavaScript boolean and radix-number coercion and locale-independent mode normalization are retained. |
| `src/ts/score-features/transposition.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreTranspositions` | done | Transposition feature normalization, MusicXML transpose XML building, and direct MusicXML transpose extraction are fully covered. JavaScript boolean/radix coercion and empty-string default handling are retained. |
| `src/ts/score-features/pitches.ts` | `jp.igapyon.mikuscore.scorefeatures.ScorePitches` | done | Pitch feature normalization, MusicXML pitch XML building, and direct MusicXML pitch extraction are fully covered. JavaScript boolean/radix coercion and empty-string default handling are retained. |
| `src/ts/score-features/note-elements.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreNoteElements` | done | Accidental, grace, stem, lyric, fingering, string-number, and technical MusicXML helper behavior are fully covered. The Java facade includes the upstream no-argument grace default and JavaScript-compatible numeric rounding. |
| `src/ts/score-features/ties.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreTies` | done | Sound tie XML, notation tied XML, and separate MusicXML tie/tied state extraction are fully covered, with locale-independent type normalization. |
| `src/ts/score-features/slurs.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreSlurs` | done | Slur XML building, slur list XML building, and direct MusicXML slur feature extraction are fully covered. Build-time raw type/placement behavior and JavaScript numeric coercion are retained. |
| `src/ts/score-features/tuplets.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreTuplets` | done | Time-modification normalization, MusicXML time-modification XML building, and direct MusicXML time-modification extraction are fully covered, with JavaScript boolean/radix numeric coercion. |
| `src/ts/score-features/ornaments.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreOrnaments` | done | Ornament kind normalization, stable MusicXML ornament XML building, tremolo normalization, and direct MusicXML ornament extraction are fully covered. Build-time raw kind/tremolo type behavior and JavaScript numeric coercion are retained. |
| `src/ts/score-features/measure-flow.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreMeasureFlow` | done | MusicXML backup and forward control XML helper behavior is fully covered, including JavaScript boolean-number coercion and XML escaping. |
| `src/ts/score-features/dynamics.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreDynamics` | done | Dynamic mark normalization, velocity-to-dynamic mapping, MusicXML direction XML building, and direct MusicXML dynamic/wedge extraction are fully covered. Build-time raw values, JavaScript numeric coercion, first-child common fields, and dynamic-before-wedge extraction order are retained. |
| `src/ts/score-features/direction-text.ts` | `jp.igapyon.mikuscore.scorefeatures.ScoreDirectionText` | done | Tempo normalization/formatting, words/tempo MusicXML direction XML building, and direct words/sound tempo/placement extraction are fully covered. Build-time raw presentation values, JavaScript numeric coercion, and locale-independent extraction normalization are retained. |
| `core/accidentalSpelling.ts` | `jp.igapyon.mikuscore.musicxml.AccidentalSpelling`, `jp.igapyon.mikuscore.musicxml.MusicXmlState` | done | Pitch spelling helper and accidental text mapping migrated; `MusicXmlState` delegates accidental text generation |
| `core/staffClefPolicy.ts` | `jp.igapyon.mikuscore.core.StaffClefPolicy`, `jp.igapyon.mikuscore.abc.AbcIo`, `jp.igapyon.mikuscore.musicxml.MusicXmlState` | done | Staff range, single-clef, pitch hysteresis, and cluster hysteresis helpers migrated; ABC clef inference and MusicXML `change_to_pitch` staff assignment delegate to the shared helper |
| `src/ts/new-score.ts` | `jp.igapyon.mikuscore.core.NewScore` | done | Runtime-independent eight-measure MusicXML template generation, piano grand staff, and bounded/radix public-option normalization are covered by `NewScoreTest` |
| `src/ts/measure-operations.ts` | `jp.igapyon.mikuscore.musicxml.MusicXmlIo` | done | Runtime-independent MusicXML text extraction, replacement, and append-measure facades are covered, including inherited timing and treble/bass grand-staff backup generation. |
| `src/ts/musicxml-output.ts` | `jp.igapyon.mikuscore.musicxml.MusicXmlOutput` | done | Runtime-independent MKS metadata-family stripping, empty-container pruning, and imported ABC diagnostic summary helpers are fully covered. |
| `src/ts/render-document.ts` | — | excluded | This module exists to prepare the document consumed by Verovio. Verovio rendering and its preparation policy are excluded from the Java parity target. |
| `core/xmlUtils.ts` | `jp.igapyon.mikuscore.musicxml.MusicXmlState`, `jp.igapyon.mikuscore.core.ScoreCore` | done | `setPitch`, `setDurationValue`, `createNoteElement`, `createRestElement`, `replaceWithRestNote` including delete-to-rest position/duration preservation coverage, chord-head delete promotion, clone-based split, simple duration notation helpers, trimmed `getVoiceText` behavior (including falsey empty-voice normalization and blank-vs-missing lane matching), document/descendant selector behavior used by save validation, and temporary node-ID collision-safe unknown-attribute preservation |
| `src/ts/musicxml-io.ts` | `jp.igapyon.mikuscore.musicxml.MusicXmlIo`, `jp.igapyon.mikuscore.musicxml.MusicXmlState` | done | MusicXML DOM parse / serialize, pretty print with ECMAScript whitespace compaction/trimming, imported-text normalization subset for part-list / part id (first duplicate handling) / document-wide `part > measure` tuplet notation including missing tuplet group normalization with Node `Number()`/`Math.round()` signatures and raw existing-attribute selection / raw-location final barline, explicit implicit beam pass with Node decimal-prefix numeric parsing and ECMAScript trim semantics, render-doc node-id bundle helper (including insertion-order maps and unchecked null-prefix stringification), preview SVG id map mode helper, measure-editor document extraction with raw attribute matching/version retention, effective attribute merge, and main-document measure replacement helper including wrapped editor selection. State inspection/diff now preserves Node's empty-vs-missing part/measure/voice values, finite fractional duration, `Number()`-shaped pitch fields, and empty work-title precedence; state commands still use local DOM helpers |
| `src/ts/load-flow.ts`, `src/ts/load-input.ts` | `jp.igapyon.mikuscore.coreapi.CoreApi` | done | Value-based `LoadInputResult` and direct `LoadFlowResult` facades cover text/byte kind validation, structured MIDI diagnostics, source selector/new-score fields, extension routing, MusicXML/MXL, ABC, MIDI, MEI, LilyPond, MSCX/MSCZ, and MSCZ MusicXML fallback. Browser File/Text/ArrayBuffer plumbing and VSQX conversion are excluded. |
| `src/ts/download-flow.ts` | `jp.igapyon.mikuscore.coreapi.CoreApi`, `jp.igapyon.mikuscore.musicxml.MxlIo` | done | Browser-independent payload facade covers timestamped file names, MIME, MusicXML/MXL, SVG, JSON, ABC/MEI/LilyPond/MIDI/MuseScore, MSCZ, ZIP bundles (including ordered duplicate entry paths), MEI-version normalization, and MIDI TPQ/preset/override/grace/accent/profile/roundtrip options. Java honors Node's nullish raw-writer option resolution and provides both raw and MidiWriterJS-compatible bytes. VSQX and DOM-triggered download are excluded. |
| `src/ts/output-encoding.ts` | `jp.igapyon.mikuscore.coreapi.CoreApi` | done | Value-based MusicXML/MXL, SVG/JSON, ABC/MEI/LilyPond, raw and MidiWriterJS-compatible MIDI, MSCX/MSCZ, and ZIP text/byte output facades are covered. VSQX is excluded. The exact Writer plan-to-SMF regression is recorded in [`upstream-midi-writer-byte-source-map.md`](upstream-midi-writer-byte-source-map.md). |
| `src/ts/playback-flow.ts` | `jp.igapyon.mikuscore.midi.MidiIo` | done | Runtime-independent dense schedule compaction and pickup-aware selected-measure timeline are migrated; AudioContext execution and browser playback orchestration remain excluded. |
| `src/ts/mxl-io.ts` | `jp.igapyon.mikuscore.musicxml.MxlIo` | done | MXL container rootfile extraction/encoding, `.musicxml` and `.xml` fallback selection, missing-rootfile diagnostics, size-based stored/DEFLATE encoding, source-style UTF-8 ZIP header layout, central-directory preflight, and requested-entry compression dispatch are covered. All four pinned upstream MXL and MSCZ archives have Java extract/repackage regressions, and a timestamp-normalized pinned-Node raw-DEFLATE byte golden matches Java output. |
| `src/ts/zip-io.ts` | `jp.igapyon.mikuscore.musicxml.MxlIo`, `jp.igapyon.mikuscore.coreapi.CoreApi` | done | ZIP text extraction by extension, root-entry listing, exact entry bytes, missing-EOCD/empty/missing-entry plus central-directory range/malformed, filename-range, local-header, data-range, and requested-entry unsupported-compression diagnostics, size-based stored/DEFLATE encoding with source-style UTF-8 local/central/EOCD header layout, and two-space XML formatting at the Java download facade are covered. All four pinned upstream MXL and MSCZ archives have Java extract/repackage regressions, and a timestamp-normalized pinned-Node raw-DEFLATE byte golden matches Java output. |
| `src/ts/abc-io.ts` | `jp.igapyon.mikuscore.abc.AbcIo` | done | Fraction, ABC length token parse/format, pitch, accidental, key, tempo unit, abcjs wrapper, measure duration estimate, ABC key fifths, `%@mks` meta directive, import line processor, body text entry, voice directive tail, header parsing, voice measure meta, MusicXML export XML, part measure render context, rendered measure misc XML, rendered part measure XML, part list/body XML integration, score-partwise document wrapper, MusicXML export context, measure note XML core, note lyric/time-modification XML, note leading direction XML, measure beam XML, note notations decoration XML, body import voice stores helper, body lyric application, body field state update, body barline processing, non-playable body entry dispatch, simple body token dispatch, bracket / grace / fallback body dispatch, pending note state helper, ABC chord harmony XML helper, MusicXML to ABC harmony / lyric helper, MusicXML to ABC DOM utility helper, MusicXML to ABC lane definition helper, MusicXML to ABC meta line helper, MusicXML to ABC measure meta helper, MusicXML to ABC measure state helper including repeat times and discontinue ending supplemental metadata, MusicXML to ABC direction token helper, MusicXML to ABC note lane / timing helper, MusicXML to ABC note ornament helper, MusicXML to ABC pitch token helper, MusicXML to ABC note ornament prefix helper, MusicXML to ABC note articulation prefix helper, MusicXML to ABC note technical prefix helper, first `parseForMusicXml` / `musicXmlFromAbc` integration, initial `musicXmlToAbc` integration, MusicXML to ABC harmony / direction / lyric public integration, MusicXML to ABC grace / tie / slur public integration including voice-less grace current-lane behavior, MusicXML to ABC tuplet / time-modification public integration, MusicXML to ABC note notation / fermata prefix public integration, MusicXML to ABC trill accidental metadata public integration, MusicXML to ABC measure / diagnostic metadata public integration, MusicXML to ABC repeat / ending barline public integration, ABC quoted-string import to harmony / words direction handoff, initial ABC golden fixture roundtrip plus `roundtrip_moonlight_m13_m16_like.musicxml`, `roundtrip_triplet_m1_m4_like.musicxml`, `roundtrip_sample6_m1_m2.musicxml`, `with_following_rest.musicxml`, `with_rest_tail.musicxml`, `with_unknown.musicxml`, `underfull.musicxml`, `overfull.musicxml`, `full_with_half.musicxml`, `inherited_time_changed.musicxml`, `inherited_divisions_changed.musicxml`, `inherited_attributes.musicxml`, `mixed_voices.musicxml`, and `with_backup.musicxml` fixture expansion, ABC body tuplet timing including shorthand `(3` and explicit-ratio `(5:4:5` public import coverage, ABC body key signature roundtrip through standard `K:` / `[K:]` public coverage, duplicate `%@mks key` first-wins import public coverage, shared header `K:` export public coverage, natural accidental emission against lane key and per-part initial key public coverage, common C-clef header export/import public coverage, `%%score` grouped voices multi-staff, multi-measure, repeat / ending, lyric, attribute-boundary, multiple grouped block, mixed grouped / ungrouped, repeated-id de-duplication, malformed-id fallback, and explicit accidental annotation, and user-defined decoration, and unsupported body warning public import coverage, ABC body grace group import, ABC overlay import integration, basic / standard-shorthand / prefixed ABC body decoration pending state, ABC body navigation / wedge / dynamics decoration import, ABC richer decoration aliases, ABC overfull compatibility diagnostics, ABC body repeat / ending metadata, ABC body tie handoff, ABC chord tie handoff, missing voice measure rest fallback, grace-note occupancy exclusion, slur warning, and ABC body broken rhythm / slur handoff slices migrated; broader fixture-based parity expansion remains pending |
| `src/ts/abc-layout.ts` | `jp.igapyon.mikuscore.abc.AbcIo` | done | All four layout exports, including layout parsing and unknown-voice fallback construction now used by ABC import, have direct evidence in [`upstream-abc-layout-source-map.md`](upstream-abc-layout-source-map.md). |
| `src/ts/abc-lexer.ts` | `jp.igapyon.mikuscore.abc.AbcLexer` | done | All three lexer exports have direct source and regression evidence in [`upstream-abc-lexer-source-map.md`](upstream-abc-lexer-source-map.md). |
| `src/ts/abc-parser.ts` | `jp.igapyon.mikuscore.abc.AbcParser` | done | All pinned `abc-parser.spec.ts` cases and every exported parser helper are covered in [`upstream-abc-parser-case-map.md`](upstream-abc-parser-case-map.md); the complete ABC I/O integration and corpus evidence is in [`upstream-abc-io-case-map.md`](upstream-abc-io-case-map.md). |
| `src/ts/musescore-io.ts` | `jp.igapyon.mikuscore.musescore.MuseScoreIo`, `jp.igapyon.mikuscore.coreapi.CoreApi`, `jp.igapyon.mikuscore.musicxml.MxlIo` | done | The helper slices listed in the migration log are migrated. The public `MusicXML DOM -> MSCX` and `MSCX -> MusicXML` facade now carries score metadata, part/staff/measure/voice hierarchy, pitched chords/rests, and key/time/clef basics through `CoreApi` and `.mscx`/`.mscz` CLI I/O. Direct/CoreApi overloads now route cut-time 4/4-to-2/2 normalization on both directions, source/version metadata default/disable, explicit/default/disabled MuseScore beam assignment, compound 6/8 grouping on import, and no-readable-staff placeholder diagnostic metadata gated by `debugMetadata`. Public MSCX import reads trimmed `metaTag` work/movement/subtitle/creator/rights/date fields and the source VBox title/composer fallback for default placeholders into MusicXML work, movement, credit, and identification elements; it follows Node staff grouping by skipping empty parts, assigning declared staff IDs once, and creating individual fallback parts for unclaimed readable staffs, carries part-level transpose plus its written-key priority, emits `KeySig`/`keysig` mode with the source title/movement/VBox inference, maps `len` fractions to source-compatible capacity/implicit pickup output, maps repeat/double-barline source forms, and preserves staff-specific clef sign/line in numbered MusicXML clefs. Basic dynamic/tempo/expression/marker/jump direction import and dynamic/words export are also connected. Richer spanner, tuplet, lyric, remaining generated diagnostic-metadata semantics, and CLI option parsing still need upstream corpus parity. |
| `src/ts/midi-io.ts` | `jp.igapyon.mikuscore.midi.MidiIo` | done | First MIDI utility slices migrated: tempo / velocity clamps, program preset mapping, leading pickup time-signature normalization, MuseScore-style pickup time-signature prelude helper, mod12, key tonic pitch class, key scale pitch class, imported-note key signature inference, drum name hint mapping, dynamics-to-velocity mapping, generic track-name detection, standard title / composer meta text helpers, metric accent profile / pattern / velocity delta helpers, tick splitting helpers, pitch-to-MIDI / MIDI pitch text helpers, key-signature accidental helper, accidental text mapping, ticks-per-quarter / program / quantize-grid normalization, gcd, near-multiple, triplet-like timing, import quantize tick / grid scoring, imported note quantization, controller velocity scaling, auto voice allocation, imported voice cluster measure segmentation, duration notation / tie / rest XML helpers, XML pretty print, MIDI hex formatting, measure / source / SysEx / diagnostic MIDI metadata miscellaneous XML helpers, measure voice MusicXML helper, part lane definition / active lane selection helpers, tempo / dynamics direction XML helpers, initial measure attributes XML helper with Viola/Vla alto-clef policy, part measure layout / pickup measure numbering helpers, part segment layout / grand-staff fallback / measure grouping helpers, tempo event measure / offset grouping helpers with upstream initial-tempo textContent parity, part definition / part-list XML helpers with track-name and MKS part-name precedence, import skeleton document XML wrapper helper, MIDI import skeleton MusicXML assembly helper, MIDI import public conversion facade with MKS/standard metadata precedence, empty-channel suppression, CC11 expression dynamics coverage, and pretty-print coverage, MusicXML playback event extraction facade including underfull / implicit pickup timeline coverage, MusicXML playback direction dynamics velocity helper, MusicXML playback grace note timing helper including classical-equal timing coverage, MusicXML playback articulation nuance helper with retrigger boundary coverage, MusicXML playback temporal expression helper, MusicXML playback metric accent option helper with 6/8, 5/4, 3-beat, fallback, and profile public extraction coverage, MusicXML playback tie processing helper with channel/pitch fallback coverage, MusicXML playback tenuto legato overlap helper, MusicXML playback default detache helper, MusicXML playback slur processing helper with playback-like merge and slur-stop boundary coverage, MusicXML playback direction wedge dynamics helper, MusicXML playback ornament expansion helper, MusicXML playback-like nuance option flags helper, MIDI variable-length / SysEx / text meta event byte writing helpers, MKS SysEx chunk text / FNV-1a fingerprint helpers, tempo / time-signature / key-signature meta event byte writing helpers, unsigned big-endian / MIDI writer velocity / raw track chunk encoding helpers, raw MIDI tempo / note / control track chunk assembly helpers, SMF header assembly helper, SMF byte reader / variable-length value / meta text decode, MKS MIDI text / SysEx metadata helpers, SMF header parser / import diagnostic carrier, SMF track summary parser / note pairing helpers, playback event parity normalization helper, drum part map / MIDI unpitched note parsing helpers with public playback coverage, MusicXML MIDI program override collection helper, measure advance / capacity div helpers, measure content span estimation helper, first-underfull pickup / implicit measure helpers, MusicXML MIDI control event collection helper with pedal CC64 public coverage, MusicXML MIDI tempo event collection helper with in-score tempo public coverage, MusicXML leading pickup ticks collection helper, MusicXML MIDI time-signature event collection helper, MusicXML MIDI key-signature event collection helper, MIDI export tempo / time-signature / key-signature event normalization helpers, MIDI export default meta-event diagnostics helper, MIDI export text metadata line helper, MIDI export playback event track grouping helper, MIDI export channel count / MKS SysEx chunk params / program preset fallback helpers, MIDI export tempo / time-signature / key-signature meta timeline helper, MIDI export playback track plan helper, MIDI export program-change / note event field helpers, MIDI export control track plan / controller-change field helpers, MIDI export writer track plan helper, MIDI export playback preparation helper, raw MIDI playback bytes assembly helper, and MIDI export playback build facade helper |
| `src/ts/midi-musescore-io.ts` | `jp.igapyon.mikuscore.midi.MidiIo` | done | Profile normalization, profile-owned TPQ/options, raw-writer preference, and safe/parity playback-mode selection are directly covered in [`upstream-midi-musescore-io-source-map.md`](upstream-midi-musescore-io-source-map.md). |
| `src/ts/mei-io.ts` | `jp.igapyon.mikuscore.mei.MeiIo` | done | XML escaping, safe integer parsing, MusicXML note type to MEI duration, alter / accidental to MEI accid, key signature, pname, lyric wordpos / syllabic, mks duration metadata, tie marker, articulation token/XML, harmony kind / degree suffix, offset tick to tstamp, MusicXML harmony values to MEI harm XML, MEI duration/type/dots import, MEI accid import, MEI key signature import, tonic-to-fifths, duration metadata, hex, tie/slur import parser, MusicXML notation fragment insertion, MEI tstamp/control endpoint, harmony text parsing, transpose/time XML, MEI dynam direction, MEI hairpin/pedal/octave/repeat/tempo/harm import, control notation application, span control application, tie carry accidental, layer event trim, source raw miscellaneous, measure meta, debug note miscellaneous, overfull diagnostic, layer body, barline, measure attributes, wrapper XML, import root/part-list, staffDef metadata, scoreDef/staffDef resolution/meter/time-symbol/clef, scoreDef-for-staff key/transpose, MEI control direction Element wrapper, repeatMark/tempo/harm Element wrapper, layer harmony/direction collection, staff/layer notation control application, staff/layer span control application, staff annot misc/measure-meta/debug, import option / initial context, initial part import state, per-measure import state, MEI note-to-MusicXML note, MEI rest event, MEI chord event, MEI note event, MEI beam container, MEI forced tuplet/grace context, MEI stem slash expansion, MEI mRest/mSpace measure duration metadata, MEI layer event parser facade, MEI parsed layer id-to-event-tick, MEI processed layer XML facade, MEI processed staff layers facade, MEI imported measure XML facade, MEI imported part XML facade, MEI score-partwise document facade, MEI public text import facade, MEI export scoreDef/staffDef scaffold helper, MEI export measure-number/document wrapper scaffold helper, MEI export measure miscellaneous/meta annotation helper, MEI export simple note/rest event helper, MEI export simple chord event helper, MEI export layer content helper, MEI export harmony DOM/staff attribute helper, MEI export direction control helper, MEI export staff timeline/gliss-slide control helper, MEI export tie/slur control helper, MEI export ornament/breath control helper, MEI export MusicXML DOM facade, MEI export facade version / transpose / tempo / dynamics parity, MEI staff-level cross-layer hairpin control-event id import parity, MEI mRest / mSpace / space public import parity, MEI beam breaksec secondary-beam split public import parity, MEI beam / graceGrp public import parity, MEI beamSpan public import parity, MEI tie-crossbar minimal note-attribute public import parity, MEI slur minimal i/m/t marker public import parity, MEI dynam minimal public import parity, MEI tuplet minimal 3:2 time-modification public import parity, MEI fermata minimal public import parity, and MEI gliss minimal start/stop public import parity slices migrated; broader MEI import/export semantics remain pending |
| `src/ts/lilypond-io.ts` | `jp.igapyon.mikuscore.lilypond.LilyPondIo` | done | First utility helper slice migrated: XML escaping, gcd / fraction reduction, LilyPond duration to ABC length token, ABC length token to LilyPond duration fallback behavior, pitch text / MIDI key helpers, and LilyPond duration / MusicXML type mapping. Basic `convertLilyPondToMusicXml` facade added for header title/composer, single staff name / `\with` instrumentName / body `\set Staff.instrumentName`, single staff `\transposition`, basic `\relative`, omitted-root `\relative` treble inference regression, native `~` tie marker and tied duration-only token handoff, native dynamics / wedge / slur / trill / glissando / pedal / bow / technical-notation handoff, native `\repeat volta` repeat barline / ending handoff, native `\alternative` ending handoff, native `\tuplet` ratio handoff, basic `\addlyrics` / standalone `\lyricmode` / single-staff and target-routed `\lyricsto` lyric handoff, multi-staff ABC voice handoff, staff-local key/time command stripping, overfull carry diagnostic message handoff, simple variable reference expansion, `\new Staff ... << ... >>` simultaneous block extraction, minimal `exportMusicXmlDomToLilyPond` part-name / `%@mks lanes` / `%@mks slur` / `%@mks transpose` / `%@mks measure` / `%@mks articul` / `%@mks accidental` / `%@mks grace` / `%@mks tuplet` / `%@mks octshift` / `%@mks trill` metadata / time signature / header title fallback / backup dense-lane chord-token handoff / multi-staff `PianoStaff` per-staff export / rest-only staff omission / F4 and inferred low-staff bass-clef export, single staff / bare block extraction, simple note/rest/barline import handoff, absolute octave baseline, basic chord token handoff, integer duration multiplier handoff, staff clef handoff, and low-range omitted-clef inference through existing `AbcIo` |
| `src/ts/vsqx-io.ts` | excluded | out of initial Java conversion scope | VSQX bridge / dependency constraints are intentionally excluded from the initial Java target |
| `src/ts/cli-api.ts` | `jp.igapyon.mikuscore.coreapi.CoreApi`, `jp.igapyon.mikuscore.cli.MikuscoreCli`, `jp.igapyon.mikuscore.musicxml.MusicXmlState`, `jp.igapyon.mikuscore.musicxml.MxlIo` | done | Text/binary result facade migrated for `decodeCliMusicXmlInput`, `encodeCliMusicXmlOutput`, `decodeCliMuseScoreInput`, `encodeCliMuseScoreOutput` (including two-space MSCZ XML normalization), `importAbcToMusicXml`, `importMeiToMusicXml`, `importLilyPondToMusicXml`, `importMidiToMusicXml`, and `importMuseScoreToMusicXml` (all with post-import MusicXML normalization), `exportMusicXmlToAbc` (including common invalid-MusicXML diagnostics), `exportMusicXmlToMei`, `exportMusicXmlToLilyPond`, `exportMusicXmlToMidi` including empty-playable failure and basic MIDI metadata handoff, `exportMusicXmlToMuseScore`, `summarizeMusicXmlState`, `inspectMusicXmlMeasure`, `validateMusicXmlCommand`, `applyMusicXmlCommand` including successful warning propagation, and `diffMusicXmlState`; Java CLI delegates matching convert/state paths including MusicXML/MXL decode and MuseScore MSCX/MSCZ file I/O through `CoreApi` |
| `scripts/miku-score-cli.mjs` | `jp.igapyon.mikuscore.cli.MikuscoreCli` | done | `--help`, `--version`, `--diagnostics text|json` including successful warnings in the JSON envelope, first `convert --from musicxml --to musicxml` slice, first `convert --from abc --to musicxml` slice, first `convert --from abc --to midi` slice, first `convert --from musicxml --to abc` slice, first `convert --from mei --to musicxml` slice, first `convert --from musicxml --to mei` slice, first `convert --from lilypond --to musicxml` slice, first `convert --from midi --to musicxml` slice, first `convert --from musicxml --to midi` slice, constrained `render svg` unsupported recognition, `state summarize`, `state inspect-measure`, `state validate-command` / `--command-file`, `state apply-command` / `--command-file`, and `state diff` with MusicXML/MXL inputs |

## Recent Coverage Notes

- 2026-05-14: `MidiIo` public MIDI import behavior has focused regression coverage for no false staccato inference on detached notes and beam assignment across rests / beat boundaries.
- 2026-05-14: `MidiIo` now treats generic MIDI track names as fallback names during part definition, preserving upstream channel 10 drum naming while keeping explicit non-generic track names authoritative.
- 2026-05-14: `MidiIo` public MIDI import behavior has focused regression coverage for FF59 key signature import, FF58 leading-pickup normalization, and triplet-aware divisions for triplet-like note timing.
- 2026-05-14: `MidiIo` public MIDI import behavior has focused regression coverage for auto quantize grid selection, MKS pickup-ticks restoration, inferred key warnings, and natural accidental emission when imported notes contradict the key signature.
- 2026-05-14: `MidiIo` public MIDI import behavior has focused regression coverage for chromatic neighbor accidental spelling, grand-staff hysteresis, phantom staff suppression, and inactive voice full-rest suppression.
- 2026-05-14: `MidiIo` public MIDI import behavior has focused regression coverage for tempo meta import, velocity dynamics import with repeat suppression, and non-notatable duration tie splitting.
- 2026-05-14: `MidiIo` public MIDI import behavior has focused regression coverage for debug MIDI meta fields, raw source metadata, MKS SysEx metadata import, and debug metadata disable option.
- 2026-05-14: `MidiIo` public MIDI import behavior has focused regression coverage for warning diagnostics metadata and raw source metadata suppression option.
- 2026-05-14: `MidiIo` MusicXML -> MIDI export helpers have focused regression coverage for key signature event collection, time signature event collection, and pickup time-signature prelude normalization.
- 2026-05-14: `MidiIo` raw MIDI export behavior has focused regression coverage for MKS text metadata suppression, standard title text metadata, and FF03 note-track name metadata.

## Out of Scope Initially

| Upstream area | Java handling |
| --- | --- |
| Web UI source | Out of Java initial conversion scope |
| Browser DOM flow | Out of Java initial conversion scope |
| Browser download / preview flow | Review only when it exposes product semantics |
