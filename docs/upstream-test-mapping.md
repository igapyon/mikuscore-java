# Upstream Test Mapping

This document maps upstream `mikuscore` test intent to Java tests.

## Mapping Status

| Upstream test | Java test | Status | Notes |
| --- | --- | --- | --- |
| `tests/unit/core.spec.ts` | `MusicXmlStateTest` | partial | Basic command catalog validation/apply subset, `change_to_pitch` grand-staff staff assignment subset, voice-specific non-primary edit regressions, insert voice/lane rejection no-target regressions, backup / forward structural rejection and allowed-edit regressions, `ui_noop` no-dirty behavior, overfull validation subset, invalid duration / pitch payload rejection without changed targets, failed-command atomicity after a successful edit, `insert_note_after` underfull warning and stable insertion regressions, `change_duration` triplet-without-context rejection, following-rest consume and underfull trailing-rest fill subset, `split_note` overfull timing revalidation subset, structural boundary subset, chord-head delete promotion, delete-to-rest preservation regressions, delete-to-rest node-id stability, unsupported note-kind no-target rejection, unknown-element preservation, beam preservation, grace-note-without-duration save allowance, missing-voice no-op save and edit recovery, grand-staff same-voice backup save allowance, and save-time invalid fixture coverage for `invalid_note_duration.musicxml`, `invalid_note_voice.musicxml`, `invalid_note_pitch.musicxml`, `invalid_rest_with_pitch.musicxml`, `invalid_chord_without_pitch.musicxml`, and `overfull.musicxml` |
| `tests/unit/accidental-spelling.spec.ts` | `AccidentalSpellingTest` | migrated | Key signature alter lookup, MIDI pitch spelling with key preference, accidental text resolution, and alter-to-text mapping covered |
| `core/staffClefPolicy.ts` helper intent | `StaffClefPolicyTest` | migrated | Grand-staff range detection, single-clef choice, pitch hysteresis, and cluster hysteresis thresholds covered |
| `tests/unit/musicxml-io.spec.ts` | `MusicXmlIoTest` | partial | Parse failure shape, part-list / part id normalization, tuplet notation enrichment including missing tuplet group normalization in a lane with existing explicit tuplet tags, explicit implicit beam pass, final right barline normalization, invalid-input passthrough, render-doc node-id cloning, measure-editor effective attribute extraction, and main-document measure replacement behavior |
| `tests/unit/preview-flow.spec.ts` | `MusicXmlIoTest` | partial | Pure preview SVG id map preparation helper intent covered for direct map, sequential fallback map, and empty rendered-id direct mode |
| `tests/unit/playback-flow.spec.ts` | `MidiIoTest` | partial | Pure dense synth schedule compaction helper intent covered for ultra-short event dropping, onset caps, global event budget, protected small onsets, and outer / unique pitch retention |
| `tests/unit/load-flow.spec.ts` | `CoreApiTest` | partial | File-extension load routing intent covered for MusicXML, MXL, MIDI, MEI, LilyPond, MuseScore MSCX/MSCZ, MSCZ MusicXML fallback, missing file, unsupported extension, and Java-side unavailable VSQX diagnostics |
| `tests/unit/download-flow.spec.ts` | `CoreApiTest`, `MxlIoTest` | partial | Browser-independent download payload intent covered for file names, content types, MusicXML/MXL, SVG, JSON, VSQX formatting, ABC/MEI/LilyPond/MIDI/MuseScore export payloads, MSCZ compression, invalid-conversion null results, and ZIP helper behavior already covered in `MxlIoTest` |
| `tests/unit/cffp-series.spec.ts` | `CoreApiTest` | partial | Minimal Java core cross-format baseline covered for MusicXML -> ABC / MEI / LilyPond / MIDI -> MusicXML first pitched note pitch, start position, and non-MIDI duration preservation; MuseScore and VSQX CFFP paths remain outside this Java baseline |
| `tests/unit/beam-common.spec.ts` | `MusicXmlIoTest` | partial | Upstream `buildMusicXmlBeamItemsXml` helper intent covered for beam XML item building and zero-level empty output |
| `tests/unit/score-durations.spec.ts` | `ScoreDurationsTest` | partial | Upstream `score-features/durations.ts` helper intent covered for dot-count normalization, MusicXML dot XML item building, and direct MusicXML dot counting |
| `tests/unit/score-articulations.spec.ts` | `ScoreArticulationsTest` | partial | Upstream `score-features/articulations.ts` helper intent covered for supported kind normalization, stable MusicXML articulation XML item/group building, and direct MusicXML articulation kind extraction |
| `tests/unit/score-barlines.spec.ts` | `ScoreBarlinesTest` | partial | Upstream `score-features/barlines.ts` helper intent covered for MusicXML barline feature XML building and direct MusicXML barline feature extraction |
| `tests/unit/score-clefs.spec.ts` | `ScoreClefsTest` | partial | Upstream `score-features/clefs.ts` helper intent covered for clef feature normalization, MusicXML clef XML building, and direct MusicXML clef extraction |
| `tests/unit/score-time-signatures.spec.ts` | `ScoreTimeSignaturesTest` | partial | Upstream `score-features/time-signatures.ts` helper intent covered for time signature feature normalization, MusicXML time XML building, and direct MusicXML time signature extraction |
| `tests/unit/score-key-signatures.spec.ts` | `ScoreKeySignaturesTest` | partial | Upstream `score-features/key-signatures.ts` helper intent covered for key signature feature normalization, MusicXML key XML building, and direct MusicXML key signature extraction |
| `tests/unit/score-transposition.spec.ts` | `ScoreTranspositionsTest` | partial | Upstream `score-features/transposition.ts` helper intent covered for transposition feature normalization, MusicXML transpose XML building, and direct MusicXML transpose extraction |
| `tests/unit/score-pitches.spec.ts` | `ScorePitchesTest` | partial | Upstream `score-features/pitches.ts` helper intent covered for pitch feature normalization, MusicXML pitch XML building, and direct MusicXML pitch extraction |
| `tests/unit/score-note-elements.spec.ts` | `ScoreNoteElementsTest` | partial | Upstream `score-features/note-elements.ts` helper intent covered for accidental, grace, stem, lyric, fingering, string-number, and technical MusicXML helper behavior |
| `tests/unit/score-ties.spec.ts` | `ScoreTiesTest` | partial | Upstream `score-features/ties.ts` helper intent covered for sound tie XML, notation tied XML, and separate MusicXML tie/tied state extraction |
| `tests/unit/score-slurs.spec.ts` | `ScoreSlursTest` | partial | Upstream `score-features/slurs.ts` helper intent covered for slur XML building, slur list XML building, and direct MusicXML slur feature extraction |
| `tests/unit/score-tuplets.spec.ts` | `ScoreTupletsTest` | partial | Upstream `score-features/tuplets.ts` helper intent covered for time-modification normalization, MusicXML time-modification XML building, and direct MusicXML time-modification extraction |
| `tests/unit/score-ornaments.spec.ts` | `ScoreOrnamentsTest` | partial | Upstream `score-features/ornaments.ts` helper intent covered for ornament kind normalization, stable MusicXML ornament XML building, tremolo normalization, and direct MusicXML ornament extraction |
| `tests/unit/score-measure-flow.spec.ts` | `ScoreMeasureFlowTest` | partial | Upstream `score-features/measure-flow.ts` helper intent covered for MusicXML backup and forward control XML helper behavior |
| `tests/unit/score-dynamics.spec.ts` | `ScoreDynamicsTest` | partial | Upstream `score-features/dynamics.ts` helper intent covered for dynamic mark normalization, velocity-to-dynamic mapping, MusicXML direction XML building, and direct MusicXML dynamic/wedge extraction |
| `tests/unit/score-direction-text.spec.ts` | `ScoreDirectionTextTest` | partial | Upstream `score-features/direction-text.ts` helper intent covered for tempo normalization/formatting, words/tempo MusicXML direction XML building, and direct words/sound tempo/placement extraction |
| `tests/unit/abc-inline-voice-switch.spec.ts` | `AbcIoTest` | migrated | Standalone `[V:...]` body lines staying active for following ABC body lines covered |
| `tests/unit/mikuscore-cli.spec.ts` MXL / ZIP fixture intent | `MxlIoTest`, `CoreApiTest`, `MikuscoreCliTest` | partial | MXL make/extract roundtrip, MSCZ make/extract roundtrip, container root-file extraction, fallback MusicXML extraction, extension-based text extraction, root-entry listing, first CLI `.mxl` input/output coverage, and CoreApi MusicXML/MuseScore file I/O facade coverage |
| `tests/unit/mikuscore-cli.spec.ts` convert bridge intent | `MikuscoreCliTest` | partial | Java CLI convert bridges covered for ABC->MusicXML, ABC->MIDI, MEI->MusicXML, MusicXML->MEI, LilyPond->MusicXML, MusicXML->LilyPond, MIDI->MusicXML, MusicXML->MIDI, MuseScore->MusicXML including MSCZ input, MusicXML->MuseScore including MSCZ output, MusicXML->ABC, and MusicXML passthrough/MXL I/O |
| `tests/unit/abc-io.spec.ts` utility intent | `AbcIoTest` | partial | Fraction helpers, ABC length token parse/format, pitch, accidental, key, tempo unit, abcjs wrapper, measure duration estimate, ABC key fifths, `%@mks` meta directive helpers, import line processor helpers, body text entry helpers, voice directive tail helpers, header parsing helpers, voice measure meta helpers, MusicXML export XML helpers, part measure render context helpers, rendered measure misc XML helpers, rendered part measure XML helper composition, part list/body XML assembly, score-partwise document wrapper, export context, parsed-to-document assembly, measure note XML core subset, note lyric/time-modification XML subset, note leading direction XML subset, measure beam XML subset, note notations decoration XML subset, body import voice stores helper subset, body lyric application subset, body field state update subset, body barline processing subset, non-playable body entry dispatch subset, simple body token dispatch subset, bracket / grace / fallback body dispatch subset, pending note state helper subset, ABC chord harmony XML helper subset, MusicXML to ABC harmony / lyric helper subset, MusicXML to ABC DOM utility helper subset, MusicXML to ABC lane definition helper subset, MusicXML to ABC meta line helper subset, MusicXML to ABC measure meta helper subset, MusicXML to ABC measure state helper subset, MusicXML to ABC direction token helper subset, MusicXML to ABC note lane / timing helper subset, MusicXML to ABC note ornament helper subset, MusicXML to ABC pitch token helper subset, MusicXML to ABC note ornament prefix helper subset, MusicXML to ABC note articulation prefix helper subset, and MusicXML to ABC note technical prefix helper subset covered |
| `tests/unit/abc-io.spec.ts` conversion intent | `AbcIoTest` | partial | First ABC body import and MusicXML export integration covered for basic headers, notes, rests, chords, barline-separated measures, slash length shorthand including `//`, numerator-slash shorthand for notes / chords / grace groups, tempo roundtrip via ABC `Q:` header, metronome beat-unit export and last-leading-tempo selection, same-staff multi-voice roundtrip overfull avoidance, ABC clef inference and bare `V:` clef shorthand / aliases, `V:` tail diagnostics and chromatic transpose import, public overfull compatibility reflow and strict-mode save failure behavior, inline field import for `[K:]` / `[M:]` / `[L:]` / `[Q:]`, continued body `K:` handling, continued header warning diagnostics, inline `[V:...]` voice-switch routing, `%%score` grouped voices multi-staff import, multi-measure staff alignment, repeat / ending marker restoration, grouped-staff lyric attachment, attribute-boundary changes, multiple grouped blocks, mixed grouped / ungrouped ordering, repeated-id de-duplication, malformed-id fallback, and explicit accidental annotation, and user-defined decoration, unsupported body warning, unsupported octave range warning / skip behavior including grace notes, invalid note/chord/grace length warning / skip behavior, malformed accidental / bounded body punctuation warning behavior, public overlay voice placement behavior, public ornament / trill alias behavior, public repeat / ending restoration behavior, public turn alias / slashed-turn behavior, public spanner / tremolo ornament behavior, public phrase / articulation alias behavior, public bowing / technical alias behavior, public mordent / arpeggiate / navigation direction behavior, public wedge / dynamics behavior, public beam behavior, public ornament canonical roundtrip behavior, public spanner export roundtrip behavior, public articulation canonical roundtrip behavior, public articulation export roundtrip behavior, public articulation alias canonical roundtrip behavior, public wedge export / canonical roundtrip behavior, public dynamics export roundtrip behavior, public bowing / tongue / heel / toe export and canonical roundtrip behavior, public fingering / string / pluck / open / snap export and canonical roundtrip behavior, public harmonic / stopped / thumb export and canonical roundtrip behavior, and public mordent / arpeggiate / schleifer / shake export and canonical roundtrip behavior, public navigation direction export and canonical roundtrip behavior, public slur / tie export and roundtrip behavior, public editorial / courtesy accidental export and roundtrip behavior, public accidental emission boundary behavior, public stopped symbol alias canonical behavior, public grace-without-ornament export and roundtrip behavior, public turn with slash grace export and roundtrip behavior, public turn / inverted-turn export and roundtrip behavior, public wavy-line start export behavior, public phrase mark export and roundtrip behavior, public staccatissimo export behavior, public digit fingering export behavior, public pluck export behavior, public string export behavior, public harmonic export behavior, public thumb-position export behavior, public mordent export behavior, public inverted-mordent export behavior, public arpeggiate export behavior, public schleifer export behavior, public shake export behavior, public segno export behavior, public coda export behavior, public fine export behavior, public dacapo export behavior, public dalsegno export behavior, public tocoda export behavior, public dacoda export behavior, public words direction export behavior, public basic harmony export behavior, public slash-bass harmony export behavior, public major-sixth harmony export behavior, quoted annotation and chord-symbol harmony import/export including unsupported chord-like annotation preservation, rehearsal decoration export/import roundtrip, ABC lyric `w:` import/export and hyphenated syllabic roundtrip, overlay voices, tuplet timing including shorthand `(3` and explicit `(5:4:5` ratio import, grace groups, basic / standard-shorthand / prefixed decorations, richer decoration aliases, accidental annotations including natural emission against lane key signatures and per-part initial key accidental emission, navigation / wedge / dynamics decorations, overfull compatibility reflow diagnostics, repeat / ending metadata including repeat times and discontinue ending supplemental metadata, measure / transpose / tuplet metadata export and restore, per-part key signature roundtrip via standard `K:` / `[K:]` fields, duplicate `%@mks key` first-wins import behavior, shared header `K:` export for common initial keys, common C-clef header export / import restoration, tie handoff, chord tie handoff, missing voice measure rest fallback, grace-note occupancy exclusion, voice-less grace note current-lane export / roundtrip behavior, slur warning, broken rhythm / slur handoff, MusicXML to ABC harmony / direction / lyric public integration, MusicXML to ABC grace / tie / slur public integration, MusicXML to ABC tuplet / time-modification public integration, MusicXML to ABC note notation / fermata prefix public integration, MusicXML to ABC trill accidental metadata roundtrip, MusicXML to ABC measure / diagnostic metadata public integration, MusicXML to ABC repeat / ending barline public integration, and ABC quoted-string import to harmony / words direction handoff |
| `tests/unit/abc-parser.spec.ts` ABC lexer-backed note intent | `AbcLexerTest` | partial | Low-level length token, accidental, and note lexing behavior covered before higher-level parser migration |
| `tests/unit/abc-parser.spec.ts` playable-event intent | `AbcParserTest` | partial | `parseAbcNoteAt`, `parseAbcChordAt`, and `parseAbcPlayableEventAt` behavior covered |
| `tests/unit/abc-parser.spec.ts` structural/body token intent | `AbcParserTest` | partial | Field, repeat, barline including ending-stop `]|`, span, terminated and unterminated decoration, both broken-rhythm directions, shorthand, tie/slur, paren/bracket, body-token, and body-entry helpers covered |
| `tests/unit/abc-parser.spec.ts` grace group intent | `AbcParserTest` | partial | Grace note groups and malformed grace accidental warning behavior covered |
| `tests/unit/abc-roundtrip-golden.spec.ts` | `AbcIoTest` | partial | Initial MusicXML -> ABC -> MusicXML golden fixture roundtrip covered for `base.musicxml`, `with_rest.musicxml`, `interleaved_voices.musicxml`, `roundtrip_piano_tempo.musicxml`, `roundtrip_moonlight_m13_m16_like.musicxml`, `roundtrip_triplet_m1_m4_like.musicxml`, `roundtrip_sample6_m1_m2.musicxml`, `with_backup_safe.musicxml`, `with_backup.musicxml`, `with_beam.musicxml`, `with_chord_timing.musicxml`, `with_following_rest.musicxml`, `with_rest_tail.musicxml`, `with_unknown.musicxml`, `underfull.musicxml`, `overfull.musicxml`, `full_with_half.musicxml`, `inherited_time_changed.musicxml`, `inherited_divisions_changed.musicxml`, `inherited_attributes.musicxml`, and `mixed_voices.musicxml`; broader fixture expansion pending |
| `tests/unit/cli-api.spec.ts` | `CoreApiTest`, `MusicXmlStateTest` | partial | `CoreApi` text/binary result facade covered for MusicXML/MXL decode/encode, MuseScore MSCZ/MSCX decode/encode, ABC / MEI / LilyPond / MIDI / MuseScore import, ABC / MEI / LilyPond / MIDI / MuseScore export, MusicXML-to-MIDI empty-playable failure, and MusicXML summary success/failure; state summary/inspection/diff plus basic command validation/apply JSON/output shape covered at MusicXML layer |
| `tests/unit/mikuscore-cli.spec.ts` | `MikuscoreCliTest` | partial | `--help`, `convert --from musicxml --to musicxml`, first `convert --from abc --to musicxml` stdin/stdout slice, first `convert --from abc --to midi` stdin/file slice, first `convert --from musicxml --to abc` stdout and file-output slice, first `convert --from mei --to musicxml` stdin/stdout slice, first `convert --from musicxml --to mei` stdin/stdout slice, first `convert --from lilypond --to musicxml` stdin/stdout slice, first `convert --from midi --to musicxml` file/stdout slice, first `convert --from musicxml --to midi` stdin/file slice, constrained `render svg` unsupported recognition, unsupported-command / unsupported-pair behavior, state commands, and basic command catalog through CLI |
| `tests/unit/musescore-io.spec.ts` | `MuseScoreIoTest`, `CoreApiTest`, `MxlIoTest` | partial | Duration type/dot helpers, default title/composer detection, repeat/dynamics/articulation/ornament/key-mode/direction-XML/clef/transpose/measure-length helpers, direction staff/voice placement, tuplet XML/tolerance/import-state/voice-utility helpers, tuplet id reference numbering helper, tuplet written duration type helper, beam level plus explicit BeamMode and implicit beam XML assignment helpers, MuseScore Slur spanner / legacy Slur id transition helpers, MuseScore imported note tie/tied and articulation/technical notation XML helpers, MuseScore Trill spanner / chord Ornament trill / accidental-mark notation XML helpers, MuseScore Ottava spanner state and display pitch shift helpers, MuseScore import pitch spelling / accidental state helpers, MuseScore imported voice accidental state by staff/pitch helpers, MuseScore imported rest / pitched note XML composition helpers, MuseScore imported voice cursor forward / clamp / tail rest helpers, MuseScore imported dynamic / direction / barline placement helpers, MuseScore imported event loop cursor step helpers, MuseScore imported beam lookup handoff helpers, MuseScore imported rest / chord event dispatch helpers, MuseScore imported grace chord duration handling, typed imported voice event carrier, typed imported voice voice-number collection helpers, small imported voice XML facade, imported voice implicit beam/tuplet/clamp regression helpers, imported staff voice loop/backup helpers, imported typed measure facade helpers, imported typed part facade helpers, imported typed document facade helpers, imported TimedEvent rest bridge helpers, MusicXML transpose / MuseScore Instrument transpose / transposed KeySig helpers, ottava helpers, import option/tick/event-routing helpers, tie/trill/chord-notation/chord-note helpers, ignored-tag/measure-overflow/parsed-measure helpers, import part-list/identification/misc/document/measure header/finalization helpers, part voice-id resolver, accidental subtype/TPC helpers, MusicXML pitch/accidental/octave-shift/direction mark payload/mid-barline repeat/notation number/tuplet time-modification/articulation/technical/clef/direction tempo export helpers, direction seed XML builder/collector, explicit clef scan helper, export source analysis/staff-count helpers, export metadata value/document body/final fallback/empty score/measure context/measure header/key signature helpers, export part identity/scaffold/instrument/result helpers, export slur id/fraction state helpers, export tuplet ref state helpers, export voice/measure voice/staff XML builders, export staff state/chord-follow merge/backup-forward cursor/by-staff push/direction routing/rest-chord construction/note cursor helpers, export chord/rest XML helpers, pending/trailing direction mark helpers, and `.mscz` / `.mscx` CLI file I/O facade are covered; broader MuseScore conversion semantics remain pending |
| `tests/unit/midi-io.spec.ts` | `MidiIoTest` | partial | First MIDI utility intent covered for tempo / velocity clamps, program preset mapping, leading pickup time-signature normalization, MuseScore-style pickup time-signature prelude helper, key pitch class helpers, imported-note key signature inference, drum name hint mapping, dynamics velocity mapping, generic track-name detection, standard title / composer meta text helpers, metric accent helpers, tick splitting helpers, pitch-to-MIDI / MIDI pitch text helpers, key-signature accidental helper, accidental text mapping, ticks-per-quarter / program / quantize-grid normalization, gcd, near-multiple, triplet-like timing, import quantize tick / grid scoring, imported note quantization, controller velocity scaling, auto voice allocation, imported voice cluster measure segmentation, duration notation / tie / rest XML helpers, XML pretty print, MIDI hex formatting, measure / source / SysEx / diagnostic MIDI metadata miscellaneous XML helpers, measure voice MusicXML helper, part lane definition / active lane selection helpers, tempo / dynamics direction XML helpers, initial measure attributes XML helper including Viola/Vla alto clef policy, part measure layout / pickup measure numbering helpers, part segment layout / grand-staff fallback / measure grouping helpers, tempo event measure / offset grouping helpers, part definition / part-list XML helpers with track-name and MKS part-name precedence, import skeleton document XML wrapper helper, MIDI import skeleton MusicXML assembly behavior, MIDI import public conversion facade behavior including MKS metadata restore and standard metadata precedence, MusicXML playback event extraction behavior, MusicXML playback direction dynamics velocity behavior, MusicXML playback grace note timing behavior, MusicXML playback articulation nuance behavior, MusicXML playback temporal expression behavior, MusicXML playback metric accent option behavior, MusicXML playback tie processing behavior, MusicXML playback tenuto legato overlap behavior, MusicXML playback default detache behavior, MusicXML playback slur processing behavior, MusicXML playback direction wedge dynamics behavior, MusicXML playback ornament expansion behavior, MusicXML playback-like nuance option flags behavior, MIDI variable-length / SysEx / text meta event byte writing helpers, MKS SysEx chunk text / FNV-1a fingerprint helpers, tempo / time-signature / key-signature meta event byte writing helpers, unsigned big-endian / MIDI writer velocity / raw track chunk encoding helpers, raw MIDI tempo / note / control track chunk assembly helpers, SMF header assembly helper, SMF byte reader / variable-length value / meta text decode, MKS MIDI text / SysEx metadata helpers, SMF header parser / import diagnostic carrier, SMF track summary parser / note pairing diagnostics, playback parity normalization helper behavior, drum part map behavior, MusicXML MIDI program override collection behavior, measure advance / capacity div behavior, measure content span estimation behavior, first-underfull pickup / implicit measure behavior, MusicXML MIDI control event collection behavior, MusicXML MIDI tempo event collection behavior, MusicXML leading pickup ticks behavior, MusicXML MIDI time-signature event collection behavior, MusicXML MIDI key-signature event collection behavior, MIDI export tempo / time-signature / key-signature event normalization behavior, MIDI export default meta-event diagnostics behavior, MIDI export text metadata line behavior, MIDI export playback event track grouping behavior, MIDI export channel count / MKS SysEx chunk params / program preset fallback behavior, MIDI export tempo / time-signature / key-signature meta timeline behavior, MIDI export playback track plan behavior, MIDI export program-change / note event field behavior, MIDI export control track plan / controller-change field behavior, MIDI export writer track plan behavior, MIDI export playback preparation behavior, raw MIDI playback bytes assembly behavior, and MIDI export playback build facade behavior |
| `tests/unit/midi-roundtrip-golden.spec.ts` | `MidiIoTest` | partial | MusicXML -> MIDI -> MusicXML golden fixture baseline covered for key, meter, and tempo preservation on `base.musicxml`, `interleaved_voices.musicxml`, and `roundtrip_piano_tempo.musicxml` |
| `tests/unit/mei-io.spec.ts` | `MeiIoTest` | partial | Helper slices covered for MusicXML-to-MEI duration/accidental/key/pitch/lyric/timing/tie/articulation mapping, harmony/tstamp XML mapping, MEI-to-MusicXML duration/accidental/key import mapping, tonic-to-fifths, duration metadata, hex, tie/slur parser behavior, MusicXML notation fragment insertion, tstamp/control endpoint resolution, harmony text parsing, transpose/time XML, MEI dynam direction XML, MEI hairpin/pedal/octave/repeat/tempo/harm import XML, MEI control notation application, MEI span control application, tie carry accidental behavior, layer event trim behavior, source raw miscellaneous fields, miscellaneous XML, measure meta parsing, debug note miscellaneous fields, overfull diagnostics, layer body, barline XML, measure attributes XML, wrapper XML, import root/part-list selection, staffDef metadata parsing, scoreDef/staffDef resolution/meter/time-symbol/clef parsing, scoreDef-for-staff key/transpose parsing, MEI control direction Element wrapper behavior, repeatMark/tempo/harm Element wrapper behavior, layer harmony/direction collection behavior, staff/layer notation control application behavior, staff/layer span control application behavior, staff annot misc/measure-meta/debug extraction behavior, import option / initial context behavior, initial part import state behavior, per-measure import state behavior, MEI note-to-MusicXML note behavior, MEI rest event behavior, MEI chord event behavior, MEI note event behavior, MEI beam container behavior, forced tuplet/grace context behavior, MEI stem slash expansion behavior, MEI mRest/mSpace duration metadata behavior, MEI layer event parser facade behavior, MEI parsed layer id-to-event-tick behavior, MEI processed layer XML facade behavior, MEI processed staff layers facade behavior, MEI imported measure XML facade behavior, MEI imported part XML facade behavior, MEI score-partwise document facade behavior, MEI public text import facade behavior including `staffDef` child label part-name import, `meter.sym=common` time-symbol import, mid-score `scoreDef` key/time/clef changes, mid-score `staffDef` key/clef/transpose changes, `scoreDef` transposition fallback when staffDef omits it, staffDef-over-scoreDef transposition precedence, staffDef child `<clef>` alto-clef import, prior alto-clef retention when later staffDef omits clef, measure-local `scoreDef` before staff content, and first-target-staff initial key/time/clef emission, MEI export scoreDef/staffDef scaffold behavior, MEI export measure-number/document wrapper scaffold behavior, MEI export measure miscellaneous/meta annotation behavior, MEI export simple note/rest event behavior, MEI export simple chord event behavior, MEI export layer content behavior, MEI export harmony DOM/staff attribute behavior, MEI export direction control behavior, MEI export staff timeline/gliss-slide control behavior, MEI export tie/slur control behavior, MEI export ornament/breath control behavior, MEI export MusicXML DOM facade behavior, MEI export facade version / transpose / tempo / dynamics behavior, staff-level cross-layer hairpin control-event id public import behavior, mRest / mSpace / space timing-preserving public import behavior, beam breaksec secondary-beam split public import behavior, beam / graceGrp public import behavior, beamSpan public import behavior, tie-crossbar minimal note-attribute public import behavior, slur minimal i/m/t marker public import behavior, dynam minimal public import behavior, tuplet minimal 3:2 time-modification public import behavior, fermata minimal public import behavior, gliss minimal start/stop public import behavior, and staff-level slur/tie startid-endid public import behavior; broader MEI import/export tests remain pending |
| `tests/unit/lilypond-io.spec.ts` | `LilyPondIoTest` | partial | First utility helper intent covered for XML escaping, fraction reduction, duration / ABC length token conversion fallback behavior, pitch text / MIDI key helpers, and LilyPond duration / MusicXML type mapping. Basic import intent covered for header title/composer, single `\new Staff` LilyPond source with staff name / `\with` instrumentName / body `\set Staff.instrumentName` / `\transposition` handoff, bare top-level music block conversion into MusicXML, absolute octave baseline behavior (`c'` -> C4, unmarked notes -> octave 3), basic `\relative c'` notation including omitted-root relative treble sample, explicit octave marks, letter-name distance resolution, and post-chord first-tone anchoring, native `~` tie marker and tied duration-only token handoff, native dynamic / wedge / slur / trill / glissando / pedal / bow / technical-notation command handoff, native `\repeat volta` repeat barline / ending import, native `\alternative` ending import, native `\tuplet` ratio import, basic `\addlyrics` / standalone `\lyricmode` / single-staff and target-routed `\lyricsto` lyric import, multi-staff MusicXML part handoff including `\with` metadata, simultaneous two-staff overcount regression / staff-local key-time command stripping, variable-based organ score import, same-staff multi-voice `%@mks lanes` metadata roundtrip, 7:8 tuplet plus 16th-run final-note roundtrip, triplet-16th type / slur visual semantics roundtrip, non-4/4 measure capacity direct import, overfull carry-to-next-measure direct import, MusicXML-to-LilyPond export text including time signature, movement-title fallback title, transpose metadata, measure metadata, articulation metadata, accidental metadata, grace metadata, tuplet metadata, octave-shift metadata, trill metadata, backup-lane validity, dense backup-lane chord selection, chord-token export, multi-staff `PianoStaff` export, non-voice1 note retention on staff, rest-only staff omission, single-staff F4 bass-clef export, and inferred low-staff bass-clef export, minimal MusicXML -> LilyPond -> MusicXML part-name roundtrip, basic `<...>` chord tokens, integer duration multiplier tokens such as `r4*3`, implicit beams for short-note groups, staff clef import for `\clef bass`, and low-range omitted-clef bass inference |
| `tests/property/core.property.spec.ts` | pending | not started | Property tests require Java-side strategy |

## Recent Coverage Notes

- 2026-05-31: `tests/unit/mei-io.spec.ts` key-signature implied accidental import is covered in `MeiIoTest`: omitted MEI `accid` still applies the active key signature as MusicXML pitch alter without emitting a visual accidental.
- 2026-05-31: `tests/unit/mei-io.spec.ts` same-measure explicit accidental carry is covered in `MeiIoTest`: a following same-pitch note without MEI `accid` keeps the active measure accidental as MusicXML pitch alter.
- 2026-05-31: `tests/unit/mei-io.spec.ts` staffDef key signature precedence is covered in `MeiIoTest`: target `staffDef key.sig` overrides `scoreDef key.sig` for MusicXML key fifths and implied pitch alter import.
- 2026-05-31: `tests/unit/mei-io.spec.ts` MEI `keysig` alias import is covered in `MeiIoTest`: dotless `keysig` on scoreDef/staffDef drives MusicXML key fifths and implied pitch alter.
- 2026-05-31: `tests/unit/mei-io.spec.ts` MEI major key inference from `key.pname` / `key.mode` is covered in `MeiIoTest`: absent keysig still resolves MusicXML key fifths and implied pitch alter.
- 2026-05-31: `tests/unit/mei-io.spec.ts` MEI minor key inference from `key.pname` / `key.mode` is covered in `MeiIoTest`: absent keysig resolves minor-mode MusicXML key fifths and implied pitch alter.
- 2026-05-31: `tests/unit/mei-io.spec.ts` MEI altered-tonic key inference from `key.pname` / `key.accid` / `key.mode` is covered in `MeiIoTest`: absent keysig resolves altered tonic MusicXML key fifths and implied pitch alter.
- 2026-05-31: `tests/unit/mei-io.spec.ts` MEI first-short-measure pickup inference is covered in `MeiIoTest`: public import marks a short first measure as MusicXML `implicit="yes"` while preserving note duration.
- 2026-05-31: `tests/unit/mei-io.spec.ts` MEI corpus default selection is covered in `MeiIoTest`: public import selects the first score-bearing child `<mei>` and carries its title and pitch content.
- 2026-05-31: `tests/unit/mei-io.spec.ts` MEI corpus empty-child fallback is covered in `MeiIoTest`: public import skips a header-only first child `<mei>` and imports the next score-bearing child.
- 2026-05-31: `tests/unit/mei-io.spec.ts` MEI corpus index selection is covered in `MeiIoTest`: public import with `meiCorpusIndex=1` selects the second child and preserves its title and pitch content.
- 2026-05-31: `tests/unit/mei-io.spec.ts` MEI corpus index bounds handling is covered in `MeiIoTest`: public import rejects out-of-range `meiCorpusIndex` with an explicit error.
- 2026-05-31: `tests/unit/mei-io.spec.ts` staff-level MEI `slur` / `tie` `startid` / `endid` control events are covered in `MeiIoTest`: public import attaches MusicXML slur and tie/tied start-stop notations to the referenced notes.
- 2026-05-31: `tests/unit/mei-io.spec.ts` staff-level MEI `tie startid` resolution from a chord child note `xml:id` is covered in `MeiIoTest`: public import attaches the tie start to the first MusicXML note emitted from the chord and the tie stop to the referenced following note.
- 2026-05-31: `tests/unit/mei-io.spec.ts` staff-level MEI tie-control accidental carry is covered in `MeiIoTest`: public import keeps the tied stop note pitch alter when the stop note omits MEI `accid`.
- 2026-05-31: `tests/unit/mei-io.spec.ts` staff-level MEI `slur` / `tie` `tstamp` / `tstamp2` control events are covered in `MeiIoTest`: public import resolves beat timestamps to the matching notes and attaches MusicXML slur plus tie/tied start-stop notations.
- 2026-05-31: `tests/unit/mei-io.spec.ts` layer-level MEI `slur` / `tie` `startid` / `endid` control events are covered in `MeiIoTest`: public import handles control events nested inside a layer and attaches MusicXML slur plus tie/tied start-stop notations.
- 2026-05-31: `tests/unit/mei-io.spec.ts` note-level MEI `tie` / `slur` attributes are covered together in `MeiIoTest`: public import emits MusicXML tie/tied and numbered slur notations on the same notes.
- 2026-05-31: `tests/unit/mei-io.spec.ts` MEI control-event `plist` fallback and `plist` + `tstamp2` slur span behavior are covered in `MeiIoTest`: public import resolves plist targets when `startid` / `endid` are absent.
- 2026-05-31: `tests/unit/mei-io.spec.ts` MEI `accid.ges` sounding alter import is covered in `MeiIoTest`: public import writes pitch alter without a visual accidental and applies chord child note `accid.ges` independently.
- 2026-05-31: `tests/unit/mei-io.spec.ts` MEI fermata, turn/mordent, and breath/caesura control-event imports are covered in `MeiIoTest`: public import attaches MusicXML notation children by timestamp or target id.
- 2026-05-14: `tests/unit/midi-io.spec.ts` public import note-notation regressions are covered in `MidiIoTest`: detached MIDI notes do not infer staccato, grouped short notes get beam tags, and beams split across rests / beat boundaries.
- 2026-05-14: `tests/unit/midi-io.spec.ts` public import note pairing / part split regressions are covered in `MidiIoTest`: same-pitch retrigger pairing is stable, overlapping notes split into voices, same-channel cross-track input creates separate parts, and channel 10 imports as a drum part.
- 2026-05-14: `tests/unit/midi-io.spec.ts` public import key/time/quantize regressions are covered in `MidiIoTest`: FF59 key signature import, FF58 leading-pickup normalization, and triplet-aware divisions for triplet-like timing.
- 2026-05-14: `tests/unit/midi-io.spec.ts` public import auto-grid / pickup metadata / key inference regressions are covered in `MidiIoTest`: straight eighth auto grid, triplet-like auto grid, MKS pickup-ticks restoration, inferred key warning, and natural accidental against key signature.
- 2026-05-14: `tests/unit/midi-io.spec.ts` public import accidental spelling / staff layout regressions are covered in `MidiIoTest`: C# lower-neighbor spelling, grand-staff split hysteresis, one-sided melody single-staff output, and inactive voice full-rest suppression.
- 2026-05-14: `tests/unit/midi-io.spec.ts` public import tempo / dynamics / duration regressions are covered in `MidiIoTest`: tempo meta direction/sound import, velocity dynamics repeat suppression, and non-notatable duration tie splitting with valid type tags.
- 2026-05-14: `tests/unit/midi-io.spec.ts` public import debug/source/SysEx metadata regressions are covered in `MidiIoTest`: debug MIDI meta fields, raw source hex metadata, MKS SysEx metadata import, and debug metadata disable option.
- 2026-05-14: `tests/unit/midi-io.spec.ts` public import warning diagnostics / source metadata option regressions are covered in `MidiIoTest`: `MIDI_POLYPHONY_VOICE_ASSIGNED` is written to `mks:diag:*`, and `sourceMetadata=false` suppresses raw source metadata.
- 2026-05-14: `tests/unit/midi-io.spec.ts` MusicXML -> MIDI key/time signature export regressions are covered in `MidiIoTest`: FF59 key signature event collection, FF58 time signature event collection, and MuseScore-style pickup prelude normalization.
- 2026-05-14: `tests/unit/midi-io.spec.ts` raw MIDI text metadata export regressions are covered in `MidiIoTest`: `emitMksTextMeta=false` suppresses `mks:` text metadata, standard `title:` text metadata remains, and FF03 note-track names are emitted.
- 2026-05-14: `tests/unit/midi-io.spec.ts` playback timeline regressions are covered in `MidiIoTest`: non-implicit underfull measures advance by full measure length, and an underfull bar followed by an implicit pickup is not double-counted.
- 2026-05-14: `tests/unit/midi-io.spec.ts` classical-equal grace timing regression is covered in `MidiIoTest`: `graceTimingMode=classical_equal` splits the beat almost equally between grace and principal notes.
- 2026-05-14: `tests/unit/midi-io.spec.ts` tied-note playback merge regressions are covered in `MidiIoTest`: MIDI-mode tie start/stop is merged into one sustained event, including channel/pitch fallback when a continuation note omits voice.
- 2026-05-14: `tests/unit/midi-io.spec.ts` slur playback merge boundary regressions are covered in `MidiIoTest`: playback-like mode with tie/slur processing merges repeated same-pitch slur notes, and slur-start boundary notes still retrigger.
- 2026-05-14: `tests/unit/midi-io.spec.ts` slur-stop / articulation retrigger regressions are covered in `MidiIoTest`: slur-stop notes do not extend into following same-pitch notes, and staccato / tenuto repeated slur notes retrigger in playback-like mode.
- 2026-05-14: `tests/unit/midi-io.spec.ts` underfull timeline / metric accent regressions are covered in `MidiIoTest`: underfull + implicit + regular-underfull playback timeline stays stable, and 6/8 plus 5/4 metric accent velocities match upstream.
- 2026-05-14: `tests/unit/midi-io.spec.ts` metric accent profile regressions are covered in `MidiIoTest`: 3-beat / fallback patterns and subtle / balanced / strong profile velocity deltas match upstream public playback extraction.
- 2026-05-14: `tests/unit/midi-io.spec.ts` in-score tempo / pedal / drum playback regressions are covered in `MidiIoTest`: in-score tempo changes keep default tick-0 tempo and emit later tick changes, pedal `change` emits CC64 off/on, and drum playback maps `midi-unpitched` plus instrument-name hints.
- 2026-05-14: `tests/unit/midi-io.spec.ts` MIDI import empty-channel / expression regressions are covered in `MidiIoTest`: program-only channels do not create empty parts, and CC11 expression changes are reflected in imported dynamics estimation.
- 2026-05-14: `tests/unit/midi-io.spec.ts` MIDI import pretty-print regression is covered in `MidiIoTest`: imported MusicXML remains pretty-printed when debug metadata is enabled; Java has no separate `debugPrettyPrint` option.
- 2026-05-14: `tests/unit/midi-io.spec.ts` has been checked through the end of the current upstream file. Export / roundtrip metadata and playback timing candidates at the tail are already represented by existing `MidiIoTest` regressions or documented Java runtime differences.
- 2026-05-14: `tests/unit/abc-io.spec.ts` grace missing-voice lane regression is covered in `AbcIoTest`: MusicXML grace notes without `<voice>` export as current-lane ABC grace groups, do not create an extra `V:P1_v2`, and roundtrip as a single part with a grace note.
- 2026-05-14: `tests/unit/abc-io.spec.ts` repeat times metadata regressions are covered in `AbcIoTest`: MusicXML backward repeat `times=3` exports standard ABC repeat plus `%@mks measure ... times=3`, and ABC import restores `times="3"` from the supplemental metadata.
- 2026-05-14: `tests/unit/abc-io.spec.ts` discontinue ending metadata regressions are covered in `AbcIoTest`: MusicXML discontinue endings export standard ABC `[1` plus `%@mks measure ... ending-type=discontinue`, and ABC import merges notation metadata with the supplemental metadata to restore both start and discontinue stop endings.
- 2026-05-14: `tests/unit/abc-io.spec.ts` tuplet shorthand / explicit ratio import regressions are covered in `AbcIoTest`: ABC `(3 DEF` imports as a 3:2 MusicXML tuplet, and `(5:4:5 C D E F G` imports as a 5:4 MusicXML tuplet.
- 2026-05-14: `tests/unit/abc-io.spec.ts` per-part key signature roundtrip regression is covered in `AbcIoTest`: mixed part keys export through standard `K:` / `[K:]` fields without `%@mks key`, and roundtrip restores each part's measure key fifths.
- 2026-05-14: `tests/unit/abc-io.spec.ts` duplicate key hint import regression is covered in `AbcIoTest`: when duplicate `%@mks key` hints exist for the same voice and measure, the first hint is kept and later duplicates do not override it.
- 2026-05-14: `tests/unit/abc-io.spec.ts` shared header K export regression is covered in `AbcIoTest`: common initial keys across lanes export as one header `K:` field and do not emit `%@mks key` or voice-local `[K:]`.
- 2026-05-14: `tests/unit/abc-io.spec.ts` natural against lane key signature export regression is covered in `AbcIoTest`: A major G natural exports as ABC `=G`.
- 2026-05-14: `tests/unit/abc-io.spec.ts` per-part initial key accidental emission regression is covered in `AbcIoTest`: the target part's initial key is used for accidental emission, including P3 A major G natural as `=G`.
- 2026-05-14: `tests/unit/abc-io.spec.ts` common C-clef header roundtrip regression is covered in `AbcIoTest`: MusicXML C clef line 3 / 4 exports as ABC `clef=alto` / `clef=tenor` and imports back as C clef line 3 / 4.
- 2026-05-25: `tests/unit/abc-io.spec.ts` spanner / tremolo ornament public regressions are covered in `AbcIoTest`: delayed turn variants, long trill wavy-line start / stop, tremolo single / start / stop, glissando / slide start / stop, and standard `!slide!` as slide start.
- 2026-05-25: `tests/unit/abc-io.spec.ts` phrase / articulation alias public regressions are covered in `AbcIoTest`: phrase marks, staccato aliases, accent / tenuto / stress / unstress / fermata, accent aliases, inverted fermata aliases, marcato / breath aliases, caesura, and staccatissimo aliases.
- 2026-05-25: `tests/unit/abc-io.spec.ts` bowing / technical alias public regressions are covered in `AbcIoTest`: bowing aliases, double / triple tongue aliases, heel / toe aliases, fingering / string / pluck decorations, open-string / snap-pizzicato aliases, stopped aliases, and thumb-position aliases.
- 2026-05-25: `tests/unit/abc-io.spec.ts` mordent / arpeggiate / navigation direction public regressions are covered in `AbcIoTest`: mordent / inverted-mordent aliases, arpeggiate aliases, schleifer / shake ornaments, segno / coda, fine, dacoda, and dacapo / dalsegno / tocoda aliases.
- 2026-05-25: `tests/unit/abc-io.spec.ts` wedge / dynamics public regressions are covered in `AbcIoTest`: crescendo / diminuendo wedge aliases and common / extreme dynamics directions from `pppp` through `ffff`, including `fp`, `fz`, `rfz`, `sf`, `sfp`, and `sfz`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` beam public regressions are covered in `AbcIoTest`: beat-boundary beam splitting, whitespace beam-break hints, and MusicXML-to-ABC beam-spacing non-preservation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` ornament canonical roundtrip public regressions are covered in `AbcIoTest`: trill aliases canonicalize to `!trill!`, `!lowerturn!` canonicalizes to `!invertedturn!`, and MusicXML turn slash / delayed-turn variants export and roundtrip.
- 2026-05-25: `tests/unit/abc-io.spec.ts` spanner export roundtrip public regressions are covered in `AbcIoTest`: tremolo single / start / stop, glissando start / stop, and slide start / stop export tokens and roundtrip restoration.
- 2026-05-25: `tests/unit/abc-io.spec.ts` articulation canonical roundtrip public regressions are covered in `AbcIoTest`: staccato aliases canonicalize to `!staccato!`, accent aliases canonicalize to `!accent!`, and inverted fermata spacing canonicalizes to `!invertedfermata!`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` articulation export roundtrip public regressions are covered in `AbcIoTest`: stress / unstress / marcato / breath / caesura MusicXML articulations export to ABC tokens and roundtrip.
- 2026-05-25: `tests/unit/abc-io.spec.ts` articulation alias canonical roundtrip public regressions are covered in `AbcIoTest`: marcato aliases canonicalize to `!marcato!`, breath aliases canonicalize to `!breath!`, and `!spiccato!` canonicalizes to `!wedge!`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` wedge export / canonical roundtrip public regressions are covered in `AbcIoTest`: MusicXML crescendo / diminuendo wedges export to named ABC decorations and symbolic wedge aliases canonicalize to named decorations.
- 2026-05-25: `tests/unit/abc-io.spec.ts` dynamics export roundtrip public regressions are covered in `AbcIoTest`: MusicXML dynamics from `pppp` through `ffff`, plus `fp`, `fz`, `rfz`, `sf`, `sfp`, and `sfz`, export to ABC tokens and roundtrip.
- 2026-05-25: `tests/unit/abc-io.spec.ts` bowing / tongue / heel / toe export and canonical roundtrip public regressions are covered in `AbcIoTest`: MusicXML technical marks export to ABC tokens and spaced / hyphenated aliases canonicalize.
- 2026-05-25: `tests/unit/abc-io.spec.ts` fingering / string / pluck / open / snap export and canonical roundtrip public regressions are covered in `AbcIoTest`: MusicXML technical values export to ABC tokens, and open-string / snap-pizzicato aliases canonicalize.
- 2026-05-25: `tests/unit/abc-io.spec.ts` harmonic / stopped / thumb export and canonical roundtrip public regressions are covered in `AbcIoTest`: MusicXML technical singleton marks export to ABC tokens, and stopped / thumb-position aliases canonicalize.
- 2026-05-25: `tests/unit/abc-io.spec.ts` mordent / arpeggiate / schleifer / shake export and canonical roundtrip public regressions are covered in `AbcIoTest`: MusicXML ornament marks export to ABC tokens, and mordent / arpeggiate aliases canonicalize.
- 2026-05-25: `tests/unit/abc-io.spec.ts` navigation direction export and canonical roundtrip public regressions are covered in `AbcIoTest`: MusicXML navigation directions export to ABC tokens, combined dacapo+tocoda roundtrips, and spaced / hyphenated navigation aliases canonicalize.
- 2026-05-25: `tests/unit/abc-io.spec.ts` slur / tie export and roundtrip public regressions are covered in `AbcIoTest`: MusicXML slurs, single-note ties, and chord ties export to ABC syntax and roundtrip back to MusicXML notation on all affected notes.
- 2026-05-25: `tests/unit/abc-io.spec.ts` editorial / courtesy accidental export and roundtrip public regressions are covered in `AbcIoTest`: MusicXML editorial and cautionary accidentals export as ABC prefixes and roundtrip back to MusicXML accidental attributes.
- 2026-05-25: `tests/unit/abc-io.spec.ts` accidental emission boundary public regressions are covered in `AbcIoTest`: MusicXML explicit accidentals are preserved when the lane key is unknown, and redundant naturals are suppressed in C major.
- 2026-05-25: `tests/unit/abc-io.spec.ts` stopped symbol alias canonical regression is covered in `AbcIoTest`: `!+!` imports as stopped technical notation and exports back as canonical `!stopped!`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` grace-without-ornament export and roundtrip public regressions are covered in `AbcIoTest`: plain grace and slash grace MusicXML notes preserve ABC grace groups without adding false trill / turn ornament notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` turn with slash grace export and roundtrip public regression is covered in `AbcIoTest`: a slash grace note and principal turn ornament survive the MusicXML -> ABC -> MusicXML path together.
- 2026-05-25: `tests/unit/abc-io.spec.ts` turn / inverted-turn export and roundtrip public regression is covered in `AbcIoTest`: plain MusicXML turn and inverted-turn ornaments export to ABC tokens and restore to MusicXML ornaments.
- 2026-05-25: `tests/unit/abc-io.spec.ts` wavy-line start export public regression is covered in `AbcIoTest`: wavy-line start without `trill-mark` exports as canonical `!trill!`, while `trill-mark` plus wavy-line start still exports as long-trill start.
- 2026-05-25: `tests/unit/abc-io.spec.ts` phrase mark export and roundtrip public regression is covered in `AbcIoTest`: MusicXML `shortphrase`, `mediumphrase`, and `longphrase` other-articulations export to ABC tokens and roundtrip back to MusicXML.
- 2026-05-25: `tests/unit/abc-io.spec.ts` staccatissimo export public regression is covered in `AbcIoTest`: MusicXML `staccatissimo` exports as canonical `!wedge!` and roundtrips without becoming `staccato`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` digit fingering export public regression is covered in `AbcIoTest`: MusicXML fingering values `0` and `5` export as standard ABC `!0!` / `!5!` decorations and roundtrip back as fingering values.
- 2026-05-25: `tests/unit/abc-io.spec.ts` pluck export public regression is covered in `AbcIoTest`: MusicXML pluck values `p`, `i`, `m`, and `a` export as ABC `!pluck:*!` decorations and roundtrip back as pluck values.
- 2026-05-25: `tests/unit/abc-io.spec.ts` string export public regression is covered in `AbcIoTest`: a single MusicXML string value `2` exports as ABC `!string:2!` and roundtrips back as a string value.
- 2026-05-25: `tests/unit/abc-io.spec.ts` harmonic export public regression is covered in `AbcIoTest`: single MusicXML harmonic technical notation exports as ABC `!harmonic!` and roundtrips back as harmonic notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` thumb-position export public regression is covered in `AbcIoTest`: single MusicXML thumb-position technical notation exports as ABC `!thumb!` and roundtrips back as thumb-position notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` mordent export public regression is covered in `AbcIoTest`: single MusicXML mordent ornament exports as ABC `!mordent!` and roundtrips back as mordent notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` inverted-mordent export public regression is covered in `AbcIoTest`: single MusicXML inverted-mordent ornament exports as canonical ABC `!pralltriller!` and roundtrips back as inverted-mordent notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` arpeggiate export public regression is covered in `AbcIoTest`: MusicXML chord arpeggiate exports as canonical ABC `!arpeggio![...]` and roundtrips back as arpeggiate notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` schleifer export public regression is covered in `AbcIoTest`: single MusicXML schleifer ornament exports as ABC `!schleifer!` and roundtrips back as schleifer notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` shake export public regression is covered in `AbcIoTest`: single MusicXML shake ornament exports as ABC `!shake!` and roundtrips back as shake notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` segno export public regression is covered in `AbcIoTest`: single MusicXML segno direction exports as ABC `!segno!` and roundtrips back as segno direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` coda export public regression is covered in `AbcIoTest`: single MusicXML coda direction exports as ABC `!coda!` and roundtrips back as coda direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` fine export public regression is covered in `AbcIoTest`: single MusicXML fine sound direction exports as ABC `!fine!` and roundtrips back as fine sound direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` dacapo export public regression is covered in `AbcIoTest`: single MusicXML dacapo sound direction exports as ABC `!dacapo!` and roundtrips back as dacapo sound direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` dalsegno export public regression is covered in `AbcIoTest`: single MusicXML dalsegno sound direction exports as ABC `!dalsegno!` and roundtrips back as dalsegno sound direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` tocoda export public regression is covered in `AbcIoTest`: single MusicXML tocoda sound direction exports as ABC `!tocoda!` and roundtrips back as tocoda sound direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` dacoda export public regression is covered in `AbcIoTest`: combined MusicXML dacapo plus tocoda sound direction exports as ABC `!dacoda!` and roundtrips back as dacapo plus tocoda sound directions.
- 2026-05-25: `tests/unit/abc-io.spec.ts` words direction export public regression is covered in `AbcIoTest`: single MusicXML words direction exports as a quoted ABC annotation and roundtrips back as words direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` basic harmony export public regression is covered in `AbcIoTest`: single MusicXML dominant harmony exports as a quoted ABC chord symbol and roundtrips back as harmony.
- 2026-05-25: `tests/unit/abc-io.spec.ts` slash-bass harmony export public regression is covered in `AbcIoTest`: altered-root / altered-bass MusicXML harmony exports as a quoted ABC slash chord symbol and roundtrips back as harmony.
- 2026-05-25: `tests/unit/abc-io.spec.ts` major-sixth harmony export public regression is covered in `AbcIoTest`: single MusicXML major-sixth harmony exports as a quoted ABC chord symbol and roundtrips back as harmony.
- 2026-05-25: `tests/unit/abc-io.spec.ts` standard digit fingering import public regression is covered in `AbcIoTest`: ABC `!0!` through `!5!` shorthand decorations import as MusicXML fingering values.
- 2026-05-25: `tests/unit/abc-io.spec.ts` harmonic import public regression is covered in `AbcIoTest`: ABC `!harmonic!` imports as MusicXML technical harmonic notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` thumb-position import public regression is covered in `AbcIoTest`: ABC `!thumb!` imports as MusicXML technical thumb-position notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` thumb-position compact alias import public regression is covered in `AbcIoTest`: ABC `!thumbpos!` imports as MusicXML technical thumb-position notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` thumb-position spaced alias import public regression is covered in `AbcIoTest`: ABC `!thumb pos!` imports as MusicXML technical thumb-position notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` thumb-position long spaced alias import public regression is covered in `AbcIoTest`: ABC `!thumb position!` imports as MusicXML technical thumb-position notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` thumb-position compact long alias import public regression is covered in `AbcIoTest`: ABC `!thumbposition!` imports as MusicXML technical thumb-position notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` thumb-position hyphen alias import public regression is covered in `AbcIoTest`: ABC `!thumb-position!` imports as MusicXML technical thumb-position notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` mordent import public regression is covered in `AbcIoTest`: ABC `!mordent!` imports as MusicXML mordent ornament notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` pralltriller import public regression is covered in `AbcIoTest`: ABC `!pralltriller!` imports as MusicXML inverted-mordent ornament notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` roll arpeggiate import public regression is covered in `AbcIoTest`: ABC `!roll!` on a chord imports as MusicXML arpeggiate notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` arpeggio alias import public regression is covered in `AbcIoTest`: ABC `!arpeggio!` on a chord imports as MusicXML arpeggiate notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` arpeggiate alias import public regression is covered in `AbcIoTest`: ABC `!arpeggiate!` on a chord imports as MusicXML arpeggiate notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` schleifer import public regression is covered in `AbcIoTest`: ABC `!schleifer!` imports as MusicXML schleifer ornament notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` shake import public regression is covered in `AbcIoTest`: ABC `!shake!` imports as MusicXML shake ornament notation.
- 2026-05-25: `tests/unit/abc-io.spec.ts` segno import public regression is covered in `AbcIoTest`: ABC `!segno!` imports as a MusicXML segno direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` coda import public regression is covered in `AbcIoTest`: ABC `!coda!` imports as a MusicXML coda direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` fine import public regression is covered in `AbcIoTest`: ABC `!fine!` imports as a MusicXML sound direction with `fine="yes"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` dacapo import public regression is covered in `AbcIoTest`: ABC `!dacapo!` imports as a MusicXML sound direction with `dacapo="yes"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` da capo alias import public regression is covered in `AbcIoTest`: ABC `!da capo!` imports as a MusicXML sound direction with `dacapo="yes"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` da-capo alias import public regression is covered in `AbcIoTest`: ABC `!da-capo!` imports as a MusicXML sound direction with `dacapo="yes"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` D.C. alias import public regression is covered in `AbcIoTest`: ABC `!D.C.!` imports as a MusicXML sound direction with `dacapo="yes"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` dalsegno import public regression is covered in `AbcIoTest`: ABC `!dalsegno!` imports as a MusicXML sound direction with `dalsegno="segno"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` dal segno alias import public regression is covered in `AbcIoTest`: ABC `!dal segno!` imports as a MusicXML sound direction with `dalsegno="segno"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` dal-segno alias import public regression is covered in `AbcIoTest`: ABC `!dal-segno!` imports as a MusicXML sound direction with `dalsegno="segno"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` D.S. alias import public regression is covered in `AbcIoTest`: ABC `!D.S.!` imports as a MusicXML sound direction with `dalsegno="segno"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` tocoda import public regression is covered in `AbcIoTest`: ABC `!tocoda!` imports as a MusicXML sound direction with `tocoda="coda"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` to coda alias import public regression is covered in `AbcIoTest`: ABC `!to coda!` imports as a MusicXML sound direction with `tocoda="coda"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` to-coda alias import public regression is covered in `AbcIoTest`: ABC `!to-coda!` imports as a MusicXML sound direction with `tocoda="coda"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` dacoda import public regression is covered in `AbcIoTest`: ABC `!dacoda!` imports as MusicXML sound directions with `dacapo="yes"` and `tocoda="coda"`.
- 2026-05-25: `tests/unit/abc-io.spec.ts` crescendo / diminuendo wedge import public regression is covered in `AbcIoTest`: ABC `!crescendo(!`, `!crescendo)!`, `!diminuendo(!`, and `!diminuendo)!` import as MusicXML wedge directions.
- 2026-05-25: `tests/unit/abc-io.spec.ts` cresc / dim / decresc wedge alias import public regression is covered in `AbcIoTest`: ABC shorthand wedge aliases import as MusicXML crescendo, diminuendo, and stop wedge directions.
- 2026-05-25: `tests/unit/abc-io.spec.ts` symbolic wedge alias import public regression is covered in `AbcIoTest`: ABC `!<(!`, `!<)!`, `!>(!`, and `!>)!` import as MusicXML wedge directions.
- 2026-05-25: `tests/unit/abc-io.spec.ts` decrescendo wedge alias import public regression is covered in `AbcIoTest`: ABC `!decrescendo(!` / `!decrescendo)!` imports as MusicXML diminuendo and stop wedge directions.
- 2026-05-25: `tests/unit/abc-io.spec.ts` decresc start wedge alias import public regression is covered in `AbcIoTest`: ABC `!decresc(!` imports as a MusicXML diminuendo wedge direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` dim stop wedge alias import public regression is covered in `AbcIoTest`: ABC `!dim)!` imports as a MusicXML stop wedge direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` sfz dynamics import public regression is covered in `AbcIoTest`: ABC `!sfz!` imports as a MusicXML `sfz` dynamics direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` sf dynamics import public regression is covered in `AbcIoTest`: ABC `!sf!` imports as a MusicXML `sf` dynamics direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` sfp dynamics import public regression is covered in `AbcIoTest`: ABC `!sfp!` imports as a MusicXML `sfp` dynamics direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` rfz dynamics import public regression is covered in `AbcIoTest`: ABC `!rfz!` imports as a MusicXML `rfz` dynamics direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` p dynamics import public regression is covered in `AbcIoTest`: ABC `!p!` imports as a MusicXML `p` dynamics direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` pp dynamics import public regression is covered in `AbcIoTest`: ABC `!pp!` imports as a MusicXML `pp` dynamics direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` ff dynamics import public regression is covered in `AbcIoTest`: ABC `!ff!` imports as a MusicXML `ff` dynamics direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` f dynamics import public regression is covered in `AbcIoTest`: ABC `!f!` imports as a MusicXML `f` dynamics direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` fff dynamics import public regression is covered in `AbcIoTest`: ABC `!fff!` imports as a MusicXML `fff` dynamics direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` fp dynamics import public regression is covered in `AbcIoTest`: ABC `!fp!` imports as a MusicXML `fp` dynamics direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` ppp dynamics import public regression is covered in `AbcIoTest`: ABC `!ppp!` imports as a MusicXML `ppp` dynamics direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` mp dynamics import public regression is covered in `AbcIoTest`: ABC `!mp!` imports as a MusicXML `mp` dynamics direction.
- 2026-05-25: `tests/unit/abc-io.spec.ts` mf dynamics import public regression is covered in `AbcIoTest`: ABC `!mf!` imports as a MusicXML `mf` dynamics direction.
- 2026-05-26: `tests/unit/abc-io.spec.ts` fz / common / extreme dynamics import public regressions are covered in `AbcIoTest`: ABC `!fz!`, common grouped dynamics, and `!pppp!` / `!ffff!` import as MusicXML dynamics directions.
- 2026-05-26: `tests/unit/abc-io.spec.ts` mordent alias import public regressions are covered in `AbcIoTest`: ABC `!lowermordent!`, `!uppermordent!`, `!pralltrill!`, `!invertedmordent!`, and `!inverted-mordent!` import as MusicXML mordent-family ornaments.
- 2026-05-26: `tests/unit/abc-io.spec.ts` stopped alias import public regressions are covered in `AbcIoTest`: ABC `!stopped!`, `!plus!`, `!stopped horn!`, and `!stopped-horn!` import as MusicXML stopped technical notation.
- 2026-05-26: `tests/unit/abc-io.spec.ts` slur notation import public regression is covered in `AbcIoTest`: ABC `(c2 d2)` imports as MusicXML slur start / stop notations on the first two pitched notes.
- 2026-05-26: `tests/unit/abc-io.spec.ts` slur-stop warning public regression is covered in `AbcIoTest`: a slur stop after a rest emits `mks:diag:count` and `mks:diag:0001` warning fields.
- 2026-05-26: `tests/unit/abc-io.spec.ts` chord tie import public regression is covered in `AbcIoTest`: ABC `[CE]-[CE]` applies `tie` / `tied` start and stop to each note in the tied chords.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC trill plus grace export public regression is covered in `AbcIoTest`: MusicXML grace notes and a principal `trill-mark` export together and roundtrip without creating a long-trill wavy-line.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC trill without grace export public regression is covered in `AbcIoTest`: a principal `trill-mark` exports as canonical `!trill!` and roundtrips without creating grace or long-trill wavy-line markup.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC turn export public regression is covered in `AbcIoTest`: a principal MusicXML `turn` ornament exports as canonical `!turn!` and roundtrips back to MusicXML turn notation.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC inverted-turn export public regression is covered in `AbcIoTest`: a principal MusicXML `inverted-turn` ornament exports as canonical `!invertedturn!` and roundtrips back to MusicXML inverted-turn notation.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC turnx variants export public regression is covered in `AbcIoTest`: MusicXML slashed turn-family ornaments export as canonical `!turnx!` / `!invertedturnx!` and roundtrip back with `slash="yes"`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC delayed turn variants export public regression is covered in `AbcIoTest`: MusicXML delayed turn-family ornaments export as canonical `!delayedturn!` / `!delayedinvertedturn!` and roundtrip back with `delayed-turn`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC delayed turn export public regression is covered in `AbcIoTest`: a single MusicXML delayed turn exports as canonical `!delayedturn!` and roundtrips back with `turn` plus `delayed-turn`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC delayed inverted-turn export public regression is covered in `AbcIoTest`: a single MusicXML delayed inverted-turn exports as canonical `!delayedinvertedturn!` and roundtrips back with `inverted-turn` plus `delayed-turn`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC tremolo export public regression is covered in `AbcIoTest`: MusicXML single / start / stop tremolos export as canonical `!tremolo-single-3!`, `!tremolo-start-2!`, and `!tremolo-stop-2!` and roundtrip back with type and marks preserved.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC single tremolo export public regression is covered in `AbcIoTest`: a single MusicXML tremolo exports as canonical `!tremolo-single-3!` and roundtrips back with type `single` and mark count `3`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC tremolo start export public regression is covered in `AbcIoTest`: a MusicXML tremolo start exports as canonical `!tremolo-start-2!` and roundtrips back with type `start` and mark count `2`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC tremolo stop export public regression is covered in `AbcIoTest`: a MusicXML tremolo stop exports as canonical `!tremolo-stop-2!` and roundtrips back with type `stop` and mark count `2`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC glissando / slide export public regression is covered in `AbcIoTest`: MusicXML glissando and slide start / stop spanners export as canonical `!gliss-start!`, `!gliss-stop!`, `!slide!`, and `!slide-stop!` and roundtrip back with start / stop notation types.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC glissando start export public regression is covered in `AbcIoTest`: a MusicXML glissando start exports as canonical `!gliss-start!` and roundtrips back with type `start`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC glissando stop export public regression is covered in `AbcIoTest`: a MusicXML glissando stop exports as canonical `!gliss-stop!` and roundtrips back with type `stop`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC slide start export public regression is covered in `AbcIoTest`: a MusicXML slide start exports as canonical `!slide!` and roundtrips back with type `start`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC slide stop export public regression is covered in `AbcIoTest`: a MusicXML slide stop exports as canonical `!slide-stop!` and roundtrips back with type `stop`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC long trill export public regression is covered in `AbcIoTest`: MusicXML `trill-mark` plus wavy-line start / stop exports as canonical `!trill(!` / `!trill)!` and roundtrips back with `trill-mark` and wavy-line start / stop.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC staccato export public regression is covered in `AbcIoTest`: a MusicXML staccato articulation exports as canonical `!staccato!` and roundtrips back to MusicXML staccato.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC accent / tenuto / fermata export public regression is covered in `AbcIoTest`: MusicXML accent, tenuto, and normal fermata export as canonical `!accent!`, `!tenuto!`, and `!fermata!` and roundtrip back to MusicXML notations.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC accent export public regression is covered in `AbcIoTest`: a MusicXML accent articulation exports as canonical `!accent!` and roundtrips back to MusicXML accent.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC tenuto export public regression is covered in `AbcIoTest`: a MusicXML tenuto articulation exports as canonical `!tenuto!` and roundtrips back to MusicXML tenuto.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC fermata export public regression is covered in `AbcIoTest`: a MusicXML normal fermata exports as canonical `!fermata!` and roundtrips back to MusicXML fermata.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC stress / unstress export public regression is covered in `AbcIoTest`: MusicXML stress and unstress articulations export as canonical `!stress!` and `!unstress!` and roundtrip back to MusicXML articulations.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC stress export public regression is covered in `AbcIoTest`: a MusicXML stress articulation exports as canonical `!stress!` and roundtrips back to MusicXML stress.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC unstress export public regression is covered in `AbcIoTest`: a MusicXML unstress articulation exports as canonical `!unstress!` and roundtrips back to MusicXML unstress.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC inverted fermata export public regression is covered in `AbcIoTest`: a MusicXML inverted fermata exports as canonical `!invertedfermata!` and roundtrips back to MusicXML inverted fermata.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC inverted fermata alias canonical public regression is covered in `AbcIoTest`: `!inverted fermata!` imports as an inverted MusicXML fermata and exports back as canonical `!invertedfermata!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC marcato / breath / caesura export public regression is covered in `AbcIoTest`: MusicXML strong-accent, breath-mark, and caesura articulations export as canonical `!marcato!`, `!breath!`, and `!caesura!` and roundtrip back to MusicXML articulations.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC marcato export public regression is covered in `AbcIoTest`: a MusicXML strong-accent articulation exports as canonical `!marcato!` and roundtrips back to MusicXML strong-accent.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC marcato `!strong accent!` alias canonical public regression is covered in `AbcIoTest`: `!strong accent!` imports as MusicXML strong-accent and exports back as canonical `!marcato!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC marcato `!strongaccent!` alias canonical public regression is covered in `AbcIoTest`: `!strongaccent!` imports as MusicXML strong-accent and exports back as canonical `!marcato!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC marcato `!strong-accent!` alias canonical public regression is covered in `AbcIoTest`: `!strong-accent!` imports as MusicXML strong-accent and exports back as canonical `!marcato!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC breath export public regression is covered in `AbcIoTest`: a MusicXML breath-mark articulation exports as canonical `!breath!` and roundtrips back to MusicXML breath-mark.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC breath-mark `!breathmark!` alias canonical public regression is covered in `AbcIoTest`: `!breathmark!` imports as MusicXML breath-mark and exports back as canonical `!breath!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC breath-mark `!breath mark!` alias canonical public regression is covered in `AbcIoTest`: `!breath mark!` imports as MusicXML breath-mark and exports back as canonical `!breath!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC breath-mark `!breath-mark!` alias canonical public regression is covered in `AbcIoTest`: `!breath-mark!` imports as MusicXML breath-mark and exports back as canonical `!breath!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC caesura export public regression is covered in `AbcIoTest`: a MusicXML caesura articulation exports as canonical `!caesura!` and roundtrips back to MusicXML caesura.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC staccatissimo export public regression is covered in `AbcIoTest`: a MusicXML staccatissimo articulation exports as canonical `!wedge!`, roundtrips back to MusicXML staccatissimo, and does not become staccato.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC staccatissimo `!spiccato!` alias canonical public regression is covered in `AbcIoTest`: `!spiccato!` imports as MusicXML staccatissimo, does not create staccato, and exports back as canonical `!wedge!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC wedge directions export public regression is covered in `AbcIoTest`: MusicXML crescendo / stop / diminuendo / stop wedge directions export as canonical `!crescendo(!`, `!crescendo)!`, `!diminuendo(!`, and `!diminuendo)!` note decorations and roundtrip back to MusicXML wedge directions.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC pppp / ffff dynamics export public regression is covered in `AbcIoTest`: MusicXML pppp and ffff dynamics export as canonical `!pppp!` and `!ffff!` decorations and roundtrip back to MusicXML dynamics.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC crescendo wedge start export public regression is covered in `AbcIoTest`: a MusicXML crescendo wedge start exports as canonical `!crescendo(!` and roundtrips back to MusicXML crescendo wedge.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC wedge `!<(!` alias canonical public regression is covered in `AbcIoTest`: `!<(!` imports as MusicXML crescendo wedge and exports back as canonical `!crescendo(!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC wedge stop export public regression is covered in `AbcIoTest`: a MusicXML wedge stop exports as a canonical stop decoration and roundtrips back to MusicXML wedge stop.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC diminuendo wedge start export public regression is covered in `AbcIoTest`: a MusicXML diminuendo wedge start exports as canonical `!diminuendo(!` and roundtrips back to MusicXML diminuendo wedge.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC wedge `!>(!` alias canonical public regression is covered in `AbcIoTest`: `!>(!` imports as MusicXML diminuendo wedge and exports back as canonical `!diminuendo(!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC wedge `!<)!` alias canonical public regression is covered in `AbcIoTest`: `!<)!` imports as MusicXML wedge stop and exports back as canonical `!crescendo)!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC wedge `!>)!` alias canonical public regression is covered in `AbcIoTest`: `!>)!` imports as MusicXML wedge stop and exports back as canonical `!diminuendo)!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC up-bow / down-bow export public regression is covered in `AbcIoTest`: MusicXML up-bow and down-bow technical markings export as canonical `!upbow!` and `!downbow!` decorations and roundtrip back to MusicXML technical markings.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC single up-bow export public regression is covered in `AbcIoTest`: MusicXML up-bow technical notation exports as canonical `!upbow!` and roundtrips back to MusicXML up-bow notation.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC single down-bow export public regression is covered in `AbcIoTest`: MusicXML down-bow technical notation exports as canonical `!downbow!` and roundtrips back to MusicXML down-bow notation.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC double-tongue / triple-tongue / heel / toe export public regression is covered in `AbcIoTest`: MusicXML technical markings export as canonical `!doubletongue!`, `!tripletongue!`, `!heel!`, and `!toe!` decorations and roundtrip back to MusicXML technical markings.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC single double-tongue export public regression is covered in `AbcIoTest`: MusicXML double-tongue technical notation exports as canonical `!doubletongue!` and roundtrips back to MusicXML double-tongue notation.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC single triple-tongue export public regression is covered in `AbcIoTest`: MusicXML triple-tongue technical notation exports as canonical `!tripletongue!` and roundtrips back to MusicXML triple-tongue notation.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC single heel export public regression is covered in `AbcIoTest`: MusicXML heel technical notation exports as canonical `!heel!` and roundtrips back to MusicXML heel notation.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC single toe export public regression is covered in `AbcIoTest`: MusicXML toe technical notation exports as canonical `!toe!` and roundtrips back to MusicXML toe notation.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC fingering / string export public regression is covered in `AbcIoTest`: MusicXML fingering values export as standard digit decorations, string values export as `!string:*!` decorations, and both roundtrip back to MusicXML technical values.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC single fingering export public regression is covered in `AbcIoTest`: a MusicXML fingering value exports as a standard digit decoration and roundtrips back to MusicXML fingering.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC digit fingering `0` / `5` export public regression is covered in `AbcIoTest`: MusicXML fingering values `0` and `5` export as standard ABC `!0!` / `!5!` decorations instead of `!fingering:*!` tokens and roundtrip back to MusicXML fingering values.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC string export public regression is covered in `AbcIoTest`: MusicXML string values export as `!string:*!` decorations and roundtrip back to MusicXML string values.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC single string export public regression is covered in `AbcIoTest`: a MusicXML string value exports as `!string:*!` and roundtrips back to MusicXML string.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC pluck export public regression is covered in `AbcIoTest`: MusicXML pluck values `p`, `i`, `m`, and `a` export as `!pluck:*!` decorations and roundtrip back to MusicXML pluck values.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC single pluck export public regression is covered in `AbcIoTest`: a MusicXML pluck value exports as a `!pluck:*!` decoration and roundtrips back to MusicXML pluck.
- 2026-05-26: `tests/unit/abc-io.spec.ts` MusicXML-to-ABC open-string export public regression is covered in `AbcIoTest`: MusicXML open-string technical notation exports as canonical `!open!` and roundtrips back to MusicXML open-string notation.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC open-string alias `!openstring!` canonical public regression is covered in `AbcIoTest`: `!openstring!` imports as MusicXML open-string technical notation and exports back as canonical `!open!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC tongue alias `!double tongue!` canonical public regression is covered in `AbcIoTest`: `!double tongue!` imports as MusicXML double-tongue technical notation and exports back as canonical `!doubletongue!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC tongue alias `!double-tongue!` canonical public regression is covered in `AbcIoTest`: `!double-tongue!` imports as MusicXML double-tongue technical notation and exports back as canonical `!doubletongue!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC tongue alias `!triple tongue!` canonical public regression is covered in `AbcIoTest`: `!triple tongue!` imports as MusicXML triple-tongue technical notation and exports back as canonical `!tripletongue!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC tongue alias `!triple-tongue!` canonical public regression is covered in `AbcIoTest`: `!triple-tongue!` imports as MusicXML triple-tongue technical notation and exports back as canonical `!tripletongue!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC toe alias `!toe mark!` canonical public regression is covered in `AbcIoTest`: `!toe mark!` imports as MusicXML toe technical notation and exports back as canonical `!toe!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC heel alias `!heel mark!` canonical public regression is covered in `AbcIoTest`: `!heel mark!` imports as MusicXML heel technical notation and exports back as canonical `!heel!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC bowing alias `!up bow!` canonical public regression is covered in `AbcIoTest`: `!up bow!` imports as MusicXML up-bow technical notation and exports back as canonical `!upbow!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC bowing alias `!up-bow!` canonical public regression is covered in `AbcIoTest`: `!up-bow!` imports as MusicXML up-bow technical notation and exports back as canonical `!upbow!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC bowing alias `!down-bow!` canonical public regression is covered in `AbcIoTest`: `!down-bow!` imports as MusicXML down-bow technical notation and exports back as canonical `!downbow!`.
- 2026-05-26: `tests/unit/abc-io.spec.ts` ABC bowing alias `!down bow!` canonical public regression is covered in `AbcIoTest`: `!down bow!` imports as MusicXML down-bow technical notation and exports back as canonical `!downbow!`.
- 2026-05-14: `tests/fixtures/with_following_rest.musicxml` is now included in the Java ABC golden fixture loop, covering a note / note / rest / note measure roundtrip invariant.
- 2026-05-14: `tests/fixtures/with_rest_tail.musicxml` is now included in the Java ABC golden fixture loop, covering a note followed by tail rests in one measure.
- 2026-05-14: `tests/fixtures/full_with_half.musicxml` is now included in the Java ABC golden fixture loop, covering mixed half / quarter durations in one measure.
- 2026-05-14: `tests/fixtures/inherited_time_changed.musicxml` is now included in the Java ABC golden fixture loop, covering a later 3/4 time-signature change.
- 2026-05-14: `tests/fixtures/inherited_divisions_changed.musicxml` is now included in the Java ABC golden fixture loop, covering a later divisions change while preserving quarter-duration sums.
- 2026-05-14: `tests/fixtures/inherited_attributes.musicxml` is now included in the Java ABC golden fixture loop, covering measure-level divisions and time-signature inheritance.
- 2026-05-14: `tests/fixtures/mixed_voices.musicxml` is now included in the Java ABC golden fixture loop, covering same-measure mixed voice layout.
- 2026-05-14: `tests/fixtures/with_backup.musicxml` is now included in the Java ABC golden fixture loop, covering same-measure backup / forward voice layout.
- 2026-05-14: `tests/fixtures/with_unknown.musicxml` is now included in the Java ABC golden fixture loop, covering unknown-tag tolerance in the invariant.
- 2026-05-14: `tests/fixtures/underfull.musicxml` is now included in the Java ABC golden fixture loop, covering underfull measure tolerance in the invariant.
- 2026-05-14: `tests/fixtures/overfull.musicxml` is now included in the Java ABC golden fixture loop, covering overfull input reflow tolerance in the invariant.
- 2026-05-14: `tests/fixtures/roundtrip_moonlight_m13_m16_like.musicxml` is now included in the Java ABC golden fixture loop, covering tuplets, backup voices, and accidentals in a two-measure fragment.
- 2026-05-14: `tests/fixtures/roundtrip_triplet_m1_m4_like.musicxml` is now included in the Java ABC golden fixture loop, covering triplet eighth runs and backup voices across four measures.
- 2026-05-14: `tests/fixtures/roundtrip_sample6_m1_m2.musicxml` is now included in the Java ABC golden fixture loop, covering multi-staff rest/chord/dynamics layout over two measures.

## Fixture Policy

- Use upstream fixtures as comparison material where practical
- Decide per fixture whether to copy into Java test resources or refer to a generated/local comparison path
- Keep local / spot tests that depend on external tools separate from primary `mvn test`
