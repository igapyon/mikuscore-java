# Upstream Test Mapping

This document maps upstream `mikuscore` test intent to Java tests.

## Mapping Status

| Upstream test | Java test | Status | Notes |
| --- | --- | --- | --- |
| `tests/unit/core.spec.ts` | `MusicXmlStateTest` | partial | Basic command catalog validation/apply subset, `change_to_pitch` grand-staff staff assignment subset, `ui_noop` no-dirty behavior, overfull validation subset, `insert_note_after` underfull warning subset, `change_duration` triplet-without-context rejection, following-rest consume and underfull trailing-rest fill subset, `split_note` overfull timing revalidation subset, structural boundary subset, and chord-head delete promotion |
| `tests/unit/accidental-spelling.spec.ts` | `AccidentalSpellingTest` | migrated | Key signature alter lookup, MIDI pitch spelling with key preference, accidental text resolution, and alter-to-text mapping covered |
| `core/staffClefPolicy.ts` helper intent | `StaffClefPolicyTest` | migrated | Grand-staff range detection, single-clef choice, pitch hysteresis, and cluster hysteresis thresholds covered |
| `tests/unit/musicxml-io.spec.ts` | `MusicXmlIoTest` | partial | Parse failure shape, part-list / part id normalization, tuplet notation enrichment including missing tuplet group normalization in a lane with existing explicit tuplet tags, explicit implicit beam pass, final right barline normalization, invalid-input passthrough, render-doc node-id cloning, measure-editor effective attribute extraction, and main-document measure replacement behavior |
| `tests/unit/mikuscore-cli.spec.ts` MXL / ZIP fixture intent | `MxlIoTest`, `CoreApiTest`, `MikuscoreCliTest` | partial | MXL make/extract roundtrip, MSCZ make/extract roundtrip, container root-file extraction, fallback MusicXML extraction, extension-based text extraction, root-entry listing, first CLI `.mxl` input/output coverage, and CoreApi MusicXML/MuseScore file I/O facade coverage |
| `tests/unit/abc-io.spec.ts` utility intent | `AbcIoTest` | partial | Fraction helpers, ABC length token parse/format, pitch, accidental, key, tempo unit, abcjs wrapper, measure duration estimate, ABC key fifths, `%@mks` meta directive helpers, import line processor helpers, body text entry helpers, voice directive tail helpers, header parsing helpers, voice measure meta helpers, MusicXML export XML helpers, part measure render context helpers, rendered measure misc XML helpers, rendered part measure XML helper composition, part list/body XML assembly, score-partwise document wrapper, export context, parsed-to-document assembly, measure note XML core subset, note lyric/time-modification XML subset, note leading direction XML subset, measure beam XML subset, note notations decoration XML subset, body import voice stores helper subset, body lyric application subset, body field state update subset, body barline processing subset, non-playable body entry dispatch subset, simple body token dispatch subset, bracket / grace / fallback body dispatch subset, pending note state helper subset, ABC chord harmony XML helper subset, MusicXML to ABC harmony / lyric helper subset, MusicXML to ABC DOM utility helper subset, MusicXML to ABC lane definition helper subset, MusicXML to ABC meta line helper subset, MusicXML to ABC measure meta helper subset, MusicXML to ABC measure state helper subset, MusicXML to ABC direction token helper subset, MusicXML to ABC note lane / timing helper subset, MusicXML to ABC note ornament helper subset, MusicXML to ABC pitch token helper subset, MusicXML to ABC note ornament prefix helper subset, MusicXML to ABC note articulation prefix helper subset, and MusicXML to ABC note technical prefix helper subset covered |
| `tests/unit/abc-io.spec.ts` conversion intent | `AbcIoTest` | partial | First ABC body import and MusicXML export integration covered for basic headers, notes, rests, chords, barline-separated measures, overlay voices, tuplet timing, grace groups, basic / standard-shorthand / prefixed decorations, richer decoration aliases, accidental annotations, navigation / wedge / dynamics decorations, overfull compatibility reflow diagnostics, repeat / ending metadata, tie handoff, chord tie handoff, missing voice measure rest fallback, grace-note occupancy exclusion, slur warning, broken rhythm / slur handoff, MusicXML to ABC harmony / direction / lyric public integration, MusicXML to ABC grace / tie / slur public integration, MusicXML to ABC tuplet / time-modification public integration, MusicXML to ABC note notation / fermata prefix public integration, MusicXML to ABC trill accidental metadata roundtrip, MusicXML to ABC measure / diagnostic metadata public integration, MusicXML to ABC repeat / ending barline public integration, and ABC quoted-string import to harmony / words direction handoff |
| `tests/unit/abc-parser.spec.ts` ABC lexer-backed note intent | `AbcLexerTest` | partial | Low-level length token, accidental, and note lexing behavior covered before higher-level parser migration |
| `tests/unit/abc-parser.spec.ts` playable-event intent | `AbcParserTest` | partial | `parseAbcNoteAt`, `parseAbcChordAt`, and `parseAbcPlayableEventAt` behavior covered |
| `tests/unit/abc-parser.spec.ts` structural/body token intent | `AbcParserTest` | partial | Field, repeat, barline including ending-stop `]|`, span, decoration, broken-rhythm, shorthand, tie/slur, paren/bracket, body-token, and body-entry helpers covered |
| `tests/unit/abc-parser.spec.ts` grace group intent | `AbcParserTest` | partial | Grace note groups and malformed grace accidental warning behavior covered |
| `tests/unit/abc-roundtrip-golden.spec.ts` | `AbcIoTest` | partial | Initial MusicXML -> ABC -> MusicXML golden fixture roundtrip covered for `base.musicxml`, `with_rest.musicxml`, `interleaved_voices.musicxml`, `roundtrip_piano_tempo.musicxml`, `with_backup_safe.musicxml`, `with_beam.musicxml`, and `with_chord_timing.musicxml`; broader fixture expansion pending |
| `tests/unit/cli-api.spec.ts` | `CoreApiTest`, `MusicXmlStateTest` | partial | `CoreApi` text/binary result facade covered for MusicXML/MXL decode/encode, MuseScore MSCZ/MSCX decode/encode, ABC / MEI / LilyPond / MIDI import, ABC / MEI / MIDI export, MusicXML-to-MIDI empty-playable failure, and MusicXML summary success/failure; state summary/inspection/diff plus basic command validation/apply JSON/output shape covered at MusicXML layer |
| `tests/unit/mikuscore-cli.spec.ts` | `MikuscoreCliTest` | partial | `--help`, `convert --from musicxml --to musicxml`, first `convert --from abc --to musicxml` stdin/stdout slice, first `convert --from abc --to midi` stdin/file slice, first `convert --from musicxml --to abc` stdout and file-output slice, first `convert --from mei --to musicxml` stdin/stdout slice, first `convert --from musicxml --to mei` stdin/stdout slice, first `convert --from lilypond --to musicxml` stdin/stdout slice, first `convert --from midi --to musicxml` file/stdout slice, first `convert --from musicxml --to midi` stdin/file slice, constrained `render svg` unsupported recognition, unsupported-command / unsupported-pair behavior, state commands, and basic command catalog through CLI |
| `tests/unit/musescore-io.spec.ts` | `MuseScoreIoTest`, `CoreApiTest`, `MxlIoTest` | partial | Duration type/dot helpers, default title/composer detection, repeat/dynamics/articulation/ornament/key-mode/direction-XML/clef/transpose/measure-length helpers, direction staff/voice placement, tuplet XML/tolerance/import-state/voice-utility helpers, tuplet id reference numbering helper, tuplet written duration type helper, beam level plus explicit BeamMode and implicit beam XML assignment helpers, MuseScore Slur spanner / legacy Slur id transition helpers, MuseScore imported note tie/tied and articulation/technical notation XML helpers, MuseScore Trill spanner / chord Ornament trill / accidental-mark notation XML helpers, MuseScore Ottava spanner state and display pitch shift helpers, MuseScore import pitch spelling / accidental state helpers, MuseScore imported voice accidental state by staff/pitch helpers, MuseScore imported rest / pitched note XML composition helpers, MuseScore imported voice cursor forward / clamp / tail rest helpers, MuseScore imported dynamic / direction / barline placement helpers, MuseScore imported event loop cursor step helpers, MuseScore imported beam lookup handoff helpers, MuseScore imported rest / chord event dispatch helpers, MuseScore imported grace chord duration handling, typed imported voice event carrier, typed imported voice voice-number collection helpers, small imported voice XML facade, imported voice implicit beam/tuplet/clamp regression helpers, imported staff voice loop/backup helpers, imported typed measure facade helpers, imported typed part facade helpers, imported typed document facade helpers, imported TimedEvent rest bridge helpers, MusicXML transpose / MuseScore Instrument transpose / transposed KeySig helpers, ottava helpers, import option/tick/event-routing helpers, tie/trill/chord-notation/chord-note helpers, ignored-tag/measure-overflow/parsed-measure helpers, import part-list/identification/misc/document/measure header/finalization helpers, part voice-id resolver, accidental subtype/TPC helpers, MusicXML pitch/accidental/octave-shift/direction mark payload/mid-barline repeat/notation number/tuplet time-modification/articulation/technical/clef/direction tempo export helpers, direction seed XML builder/collector, explicit clef scan helper, export source analysis/staff-count helpers, export metadata value/document body/final fallback/empty score/measure context/measure header/key signature helpers, export part identity/scaffold/instrument/result helpers, export slur id/fraction state helpers, export tuplet ref state helpers, export voice/measure voice/staff XML builders, export staff state/chord-follow merge/backup-forward cursor/by-staff push/direction routing/rest-chord construction/note cursor helpers, export chord/rest XML helpers, pending/trailing direction mark helpers, and `.mscz` / `.mscx` CLI file I/O facade are covered; broader MuseScore conversion semantics remain pending |
| `tests/unit/midi-io.spec.ts` | `MidiIoTest` | partial | First MIDI utility intent covered for tempo / velocity clamps, program preset mapping, leading pickup time-signature normalization, MuseScore-style pickup time-signature prelude helper, key pitch class helpers, imported-note key signature inference, drum name hint mapping, dynamics velocity mapping, generic track-name detection, standard title / composer meta text helpers, metric accent helpers, tick splitting helpers, pitch-to-MIDI / MIDI pitch text helpers, key-signature accidental helper, accidental text mapping, ticks-per-quarter / program / quantize-grid normalization, gcd, near-multiple, triplet-like timing, import quantize tick / grid scoring, imported note quantization, controller velocity scaling, auto voice allocation, imported voice cluster measure segmentation, duration notation / tie / rest XML helpers, XML pretty print, MIDI hex formatting, measure / source / SysEx / diagnostic MIDI metadata miscellaneous XML helpers, measure voice MusicXML helper, part lane definition / active lane selection helpers, tempo / dynamics direction XML helpers, initial measure attributes XML helper, part measure layout / pickup measure numbering helpers, part segment layout / grand-staff fallback / measure grouping helpers, tempo event measure / offset grouping helpers, part definition / part-list XML helpers, import skeleton document XML wrapper helper, MIDI import skeleton MusicXML assembly behavior, MIDI import public conversion facade behavior, MusicXML playback event extraction behavior, MusicXML playback direction dynamics velocity behavior, MusicXML playback grace note timing behavior, MusicXML playback articulation nuance behavior, MusicXML playback temporal expression behavior, MusicXML playback metric accent option behavior, MusicXML playback tie processing behavior, MusicXML playback tenuto legato overlap behavior, MusicXML playback default detache behavior, MusicXML playback slur processing behavior, MusicXML playback direction wedge dynamics behavior, MusicXML playback ornament expansion behavior, MusicXML playback-like nuance option flags behavior, MIDI variable-length / SysEx / text meta event byte writing helpers, MKS SysEx chunk text / FNV-1a fingerprint helpers, tempo / time-signature / key-signature meta event byte writing helpers, unsigned big-endian / MIDI writer velocity / raw track chunk encoding helpers, raw MIDI tempo / note / control track chunk assembly helpers, SMF header assembly helper, SMF byte reader / variable-length value / meta text decode, MKS MIDI text / SysEx metadata helpers, SMF header parser / import diagnostic carrier, SMF track summary parser / note pairing diagnostics, playback parity normalization helper behavior, drum part map behavior, MusicXML MIDI program override collection behavior, measure advance / capacity div behavior, measure content span estimation behavior, first-underfull pickup / implicit measure behavior, MusicXML MIDI control event collection behavior, MusicXML MIDI tempo event collection behavior, MusicXML leading pickup ticks behavior, MusicXML MIDI time-signature event collection behavior, MusicXML MIDI key-signature event collection behavior, MIDI export tempo / time-signature / key-signature event normalization behavior, MIDI export default meta-event diagnostics behavior, MIDI export text metadata line behavior, MIDI export playback event track grouping behavior, MIDI export channel count / MKS SysEx chunk params / program preset fallback behavior, MIDI export tempo / time-signature / key-signature meta timeline behavior, MIDI export playback track plan behavior, MIDI export program-change / note event field behavior, MIDI export control track plan / controller-change field behavior, MIDI export writer track plan behavior, MIDI export playback preparation behavior, raw MIDI playback bytes assembly behavior, and MIDI export playback build facade behavior |
| `tests/unit/mei-io.spec.ts` | `MeiIoTest` | partial | Helper slices covered for MusicXML-to-MEI duration/accidental/key/pitch/lyric/timing/tie/articulation mapping, harmony/tstamp XML mapping, MEI-to-MusicXML duration/accidental/key import mapping, tonic-to-fifths, duration metadata, hex, tie/slur parser behavior, MusicXML notation fragment insertion, tstamp/control endpoint resolution, harmony text parsing, transpose/time XML, MEI dynam direction XML, MEI hairpin/pedal/octave/repeat/tempo/harm import XML, MEI control notation application, MEI span control application, tie carry accidental behavior, layer event trim behavior, source raw miscellaneous fields, miscellaneous XML, measure meta parsing, debug note miscellaneous fields, overfull diagnostics, layer body, barline XML, measure attributes XML, wrapper XML, import root/part-list selection, staffDef metadata parsing, scoreDef/staffDef resolution/meter/time-symbol/clef parsing, scoreDef-for-staff key/transpose parsing, MEI control direction Element wrapper behavior, repeatMark/tempo/harm Element wrapper behavior, layer harmony/direction collection behavior, staff/layer notation control application behavior, staff/layer span control application behavior, staff annot misc/measure-meta/debug extraction behavior, import option / initial context behavior, initial part import state behavior, per-measure import state behavior, MEI note-to-MusicXML note behavior, MEI rest event behavior, MEI chord event behavior, MEI note event behavior, MEI beam container behavior, forced tuplet/grace context behavior, MEI stem slash expansion behavior, MEI mRest/mSpace duration metadata behavior, MEI layer event parser facade behavior, MEI parsed layer id-to-event-tick behavior, MEI processed layer XML facade behavior, MEI processed staff layers facade behavior, MEI imported measure XML facade behavior, MEI imported part XML facade behavior, MEI score-partwise document facade behavior, MEI public text import facade behavior, MEI export scoreDef/staffDef scaffold behavior, MEI export measure-number/document wrapper scaffold behavior, MEI export measure miscellaneous/meta annotation behavior, MEI export simple note/rest event behavior, MEI export simple chord event behavior, MEI export layer content behavior, MEI export harmony DOM/staff attribute behavior, MEI export direction control behavior, MEI export staff timeline/gliss-slide control behavior, MEI export tie/slur control behavior, MEI export ornament/breath control behavior, MEI export MusicXML DOM facade behavior, MEI export facade version / transpose / tempo / dynamics behavior, staff-level cross-layer hairpin control-event id public import behavior, mRest / mSpace / space timing-preserving public import behavior, beam breaksec secondary-beam split public import behavior, beam / graceGrp public import behavior, beamSpan public import behavior, tie-crossbar minimal note-attribute public import behavior, slur minimal i/m/t marker public import behavior, dynam minimal public import behavior, tuplet minimal 3:2 time-modification public import behavior, fermata minimal public import behavior, and gliss minimal start/stop public import behavior; broader MEI import/export tests remain pending |
| `tests/unit/lilypond-io.spec.ts` | `LilyPondIoTest` | partial | First utility helper intent covered for XML escaping, fraction reduction, duration / ABC length token conversion fallback behavior, pitch text / MIDI key helpers, and LilyPond duration / MusicXML type mapping. Basic import intent covered for header title/composer, single `\new Staff` LilyPond source with staff name / `\with` instrumentName / body `\set Staff.instrumentName` / `\transposition` handoff, bare top-level music block conversion into MusicXML, absolute octave baseline behavior (`c'` -> C4, unmarked notes -> octave 3), basic `\relative c'` notation including omitted-root relative treble sample, explicit octave marks, letter-name distance resolution, and post-chord first-tone anchoring, native `~` tie marker and tied duration-only token handoff, native dynamic / wedge / slur / trill / glissando / pedal / bow / technical-notation command handoff, native `\repeat volta` repeat barline / ending import, native `\alternative` ending import, native `\tuplet` ratio import, basic `\addlyrics` / standalone `\lyricmode` / single-staff and target-routed `\lyricsto` lyric import, multi-staff MusicXML part handoff including `\with` metadata, simultaneous two-staff overcount regression / staff-local key-time command stripping, variable-based organ score import, same-staff multi-voice `%@mks lanes` metadata roundtrip, 7:8 tuplet plus 16th-run final-note roundtrip, triplet-16th type / slur visual semantics roundtrip, non-4/4 measure capacity direct import, overfull carry-to-next-measure direct import, MusicXML-to-LilyPond export text including time signature, movement-title fallback title, transpose metadata, measure metadata, articulation metadata, accidental metadata, grace metadata, tuplet metadata, octave-shift metadata, trill metadata, backup-lane validity, dense backup-lane chord selection, and chord-token export, minimal MusicXML -> LilyPond -> MusicXML part-name roundtrip, basic `<...>` chord tokens, integer duration multiplier tokens such as `r4*3`, implicit beams for short-note groups, staff clef import for `\clef bass`, and low-range omitted-clef bass inference |
| `tests/property/core.property.spec.ts` | pending | not started | Property tests require Java-side strategy |

## Fixture Policy

- Use upstream fixtures as comparison material where practical
- Decide per fixture whether to copy into Java test resources or refer to a generated/local comparison path
- Keep local / spot tests that depend on external tools separate from primary `mvn test`
